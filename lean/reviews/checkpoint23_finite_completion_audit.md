# Checkpoint 23: finite/probabilistic completion matrix

Read-only audit by `/root/finite_formal`, against the **427 final frozen sources** (preserving the initial418 snapshot history), the combined manuscript and the earlier first-step note. This report checks the finite, probabilistic, Gaussian and Appendix A–B boundaries. It does not certify the entire paper or replace the separately assigned analytic and geometric reviews.

**Final finding.** The fixed-error cumulative formula, original Gaussian route, integral cap selection, literal source sampling and prescribed bush stages have actual checked constructions. The two additional interface issues found in the initial418 audit have now also been positively closed in the final427: SourceLocalizationGeometry supplies the joint dyadic/count/density certificate from actual midpoint geometry, and SixDimensionalLogarithmicLengths supplies the first-step Lemma6 estimate uniformly for an original logarithmic two-ends constant, arbitrary fixed density multiples and bounded individual original lengths. No further unresolved substantive mathematical requirement was identified in this assigned finite/probabilistic scope after the final signatures were read. This is a bounded source review, not a whole-paper correctness verdict or independent human validation. Final integrated verification and publication remain the parent's separate gates.

## Evidence and exact byte boundary

I re-read the source statements/displays listed below and actual final theorem signatures. Fresh proof reads particularly covered the fixed-error bins, original-data equality and same-law Lebesgue sampling transport, exact finite angle/fiber selection and grouped-pruning interfaces, the final marked-length normalization, the prescribed bush stages and Appendix B finite maximization. The Gaussian, cap and source sampling constructions also have the detailed hash-specific review in `checkpoint22_finite_completion_audit.md`; that evidence is incorporated with fresh byte verification, not described as another independent reproof of all those modules. Some modules are my own; parent/scalar/geometry reviews provide separate agent checks, not an independent human referee's certification.

The companion **`checkpoint23_finite_completion_hashes.json`** records all 427 current/frozen hashes, all five source-input hashes, 111 selected module hashes, 829 extracted theorem signatures with source line numbers, and 81 selected modules’ available exact-hash individual audit records. All 427 match the final freeze; no source was edited. This signature inventory is a locator, not a Lean parser or proof checker. The parent owns integrated verification and publication; the freeze status at capture was `frozen_awaiting_integrated_verification`. Individual PASS records and byte equality do not imply that the new integrated run has passed.

Status **P** below means a constructed theorem with the stated actual scope; **A** means a sufficient alternative proof method rather than the precise prose construction; **R** means a remaining source-statement/interface requirement. Conditional theorems remain conditional on their exact analytic input predicates.

## Finite and geometric-combinatorial requirements

| Source requirement | Exact interface and checked scope | Status |
|---|---|---|
| Corollary 1.1, (1.6), combined lines 145ff: one fixed input error, one logarithm, same cap coefficient | `CumulativeFixedError.from_fixed_input` / `source_notation`: `cr>0` depends only on dimension, normalization and `p≥1`, before `m,d,eta,c,A,deltaMax`; the comparable-family premise is required only at that same A and cutoff. Output is `cr*c*A⁻¹*δ^(m-d+eta)*s^p*M/log(2/δ)≤#union`. Actual positive-integer bins and zero class include empty/unequal rows and arbitrarily tiny s. `linear_bin_budget` exports `J+2≤B log(2/δ)` without spending more error or using log(1/s). | P, new in 23 |
| Lemma 3.1, (3.1), and first-step Lemma 7: actual smallest-scale localization | `UniformLocalization.finite_localization` / measurable localization; `FiniteGridLocalization.exists_selection`, `FiniteLocalizedFamily.family`; completed-measure `LebesgueLocalization.exists_selection`. The actual common radius, restricted original sets, population and density-class logarithms, and every relative ball test are constructed. There is no supplied favorable radius or comparable class. The final SourceLocalizationGeometry.unit_source_notation preserves the joint dyadic/population/density certificate and derives the top cover from original midpoint geometry at width1,N≥2. | P final427 |
| Lemma 3.2, (3.2), and first-step Lemma 2: angular/spatial decomposition | `MeasurableAngularDecomposition.construct`, `MeasurableAngularSpatialLengths`, `LebesgueAngularSpatial.construct`: actual original indices and restricted sets, common scale, actual containing spatial boxes/tubes, proportional mark filter, cubic-log retained mass and pointwise overlap. The latter handles NullMeasurableSet inputs and actual fixed upper-bounded original lengths. No reinstated pre-restriction full shading or supplied spatial-cover oracle is used. Detailed geometry review is separately assigned. | P |
| §3.3, (3.3)–(3.10): density/mark loss accounting | `SourceDensityPruning.source_fractions`; `LebesgueDensityPruning.source_fractions` preserves the literal fractions and original set operations on the completion. Actual survivor, piece and multiplicity filters prove the loss inequalities. Broadness is transferred after a proportional threshold; it is not claimed invariant under arbitrary thinning. | P |
| Lemma 4.2: simultaneous integral descendant capacities | `CapSelection.CapacityTree.attainsRank`, `mass_le_rank`, `fractionalSelection`, `all_capacities`: actual finite partition tree, arbitrary finite arity/natural capacities, actual selected leaves, all occurring descendant constraints. Empty support/zero capacities included. The rank recursion and fractional mass bound are proved. | P |
| §4.4, (4.24)–(4.25); Corollary 4.4, (4.30): whole-tube real-cap thinning | `CapThinningGeometry.full_cap_thinning` constructs charts, occupied hierarchy, capacities and color; `LocalizedDirectionThinning.thin_to_scale` gives literal target separation and population `M/[C A(target/δ)^m]`, unchanged full rows, absolute target cap coefficient. Nonintegral m is allowed. Geometric instantiation uses ceilings, whose fixed-factor growth is sufficient, instead of the displayed floor assignment. | P/A |
| Lemma 4.1; Corollary 4.3, (4.26)–(4.29) | `FractionalSeedFullRange.fractional_discrete_seed`, `fractional_real_cap_seed`, `source_cumulative`: actual range m>1, a single inverse-A loss, source set/density errors, and arbitrary cumulative rows. Its localization, thinning and seed inputs are derived. This is not merely a conditional scalar recurrence. | P |
| §4.4 sharpness paragraph, lines 833–844 | `RadialPencil`, `RadialSharpness.counterexamples` / `no_uniform_sublinear_cap`: actual separated radial tubes through one original cell, singleton rows and actual cap coefficient; for `1<m<ambient−1`, `gamma<1`, choose a positive error before any proposed constant so `(m-(ambient−1))(1-gamma)+2eps<0`. The basic construction allows arbitrarily small scales. The public negation does not separately accept a prescribed scale cutoff; that extra wrapper is not a missing displayed conclusion. | P |
| §5.3–5.4, (5.14), (5.16), (5.18), (5.19): actual fibers, labels and collisions | `AngleFiberSelection.select_angle_fibers` constructs a common dyadic fiber level, whole retained angle/output edges, a most-frequent label and one original angle with exactly h samples per retained output. `LegalSampleOutputs` derives geometric output/fiber bounds; `ActualLabelSelection.construct` and `CollisionEnergy.collision_energy_bound` discharge legal abundance/count and collision hypotheses from actual geometry. Samples assigned to an output are not a union over several angles. | P |
| §5.5–5.7: lifted population, grouped pruning, support and energy | `SelectedOutputSlabs`, `SelectedFamilyPruning.constructed_pruning`, `GroupedIncidence.calibrated_pruning`, downstream closing: actual lifted cells, same grid-compatible shift per pivot/slab, actual normalized colored families and incidence set. The generic pruning lemma openly takes a cumulative analytic premise; the selected-family consumer supplies it using the actual lifted estimate. It is not itself an unconditional energy theorem. | P, conditional analytic input preserved |
| Theorem 5.1 and Lemma 5.2/(5.34), plus first-step Lemma 4 | `SourceMarkedLengths.source_fourth` / `source_notation` consumes literal `SourceAnalyticInputs.Base` and `.Lifted`, raw marked input, fixed row/width/length/separation conventions, p>0 and q≥2. It derives the original fourth-power formula with κ exponent `5(ambient−1)+6q+12`, ξ exponent p+3, log exponent −(p+4), original A/M/union, and only the twentieth-power scale test. κ is an actual source minimum with its unspecified small constant chosen once for fixed normalization, including fixed factors inside `1/alpha`; no false uniform comparison with an earlier preassigned κ is asserted. | P |

The completed measurable seed and raw marked hairbrush now have `LebesgueSeedDensity` and `LebesgueHairbrush` consumers. Their exact analytic formulas and pointwise/a.e. broadness distinction are the separate geometry/analytic audits’ responsibility; the present finite matrix does not replace those reviews.

## Original Gaussian projection route: Lemma 2.1 and first-step Lemma 1

Source: combined lines 219–278, (2.1)–(2.4); first-step lines 55ff, (4)–(6). The original ambient dimension is **seven**, cap exponent four, target ambient dimension **five**.

| Required step | Actual theorem/proof boundary | Status |
|---|---|---|
| Actual standard Gaussian 5×7 matrix and independent orthogonal-pair images | `GaussianMatrix.entries_law`, `jointly_gaussian`, `orthogonal_independent`, `unit_image_law`, `orthogonal_joint_law`. Joint Gaussianity is proved before covariance gives independence; image laws are not premises. | P |
| Bounded operator and many noncollapsed directions | `GaussianLinearOperator.operator_norm_le`, `GaussianMatrixMoments`, `GaussianGoodDirections.good_probability`: actual moment 35, norm cutoff20, retained length≥1/4 for half the input directions, event probability≥17/20. Original directions need not be mutually independent. | P |
| Conditional four-dimensional small ball and (2.3) | `GaussianPerpendicular`, `GaussianConditioning.matrix_section_bound`, `ProjectiveAngleComparison.orthogonal_decomposition`, `GaussianCollision.collision_probability`. Fubini on an actual measurable perpendicular section gives exponent4; operator truncation is deterministic containment, not a Gaussian law after conditioning. Nonzero image tests and zero-first-vector fallback are explicit. Actual result is `C_K min(1,(δ/psi)^4)`. | P |
| Expected actual collision count | `GaussianCollisionKernel.family_row_bound`; `GaussianCollisionExpectation.expected_count_bound`: original separation/cap4 and actual dyadic shells give `C(K) A M log(2/δ)`. Ordered collisions exclude the diagonal; kernel inclusion of the diagonal is harmless. | P |
| Simultaneous matrix and independent subset | `GaussianRealization.exists_matrix`, `CollisionIndependentSet.exists_independent_ordered`, `GaussianCollisionSelection.select`, `GaussianProjectedSelection.exists_projection`. Same ω satisfies all events. Graph denominator is `V+orderedEdges=V+2e`, yielding `c M/[A log(2/δ)]`; empty graph/population is handled. Independent-set proof uses Turán on the complement rather than random ordering. | P/A |
| Original shading preservation under the projection | `ProjectedGridFamily.construct`, `GaussianProjectedFamily.construct`: one common rounded-label map; actual same-tube fibers bounded; actual projected bases, directions and lengths in [1/4,20]; target row≥old row/fixed C; target union≤old union. This selects labels rather than covering every point of each projected cube, a sufficient and honestly stated alternative. | P/A |
| Apply the actual five-dimensional estimate and get the literal cumulative source formula | `FiveDimensionalLengthSeed.original_length_bound`, `GaussianProjectionSeed.original_rows`, `GaussianNormalizedSeed.source_count`: `c A⁻¹ N^(-1/2-eps) s^(7/2+eps) M≤#union` from actual total incidence≥sNM, including empty rows and arbitrary s≥0. The 5D input is proved by the fractional seed; it is not an external Wolff axiom or a proof of Wolff's historical paper. | P |

Thus (2.2) is no longer only obtained by bypassing Gaussian projection. Both the alternate fractional-seed implication and an actual matrix/projection/5D closing route exist. Their historical attribution remains distinct from their checked mathematical consequence.

## Literal source sampling: Lemma 6.1, every display (6.1)–(6.19)

First-step Lemma 5 is included by the general source range. For source ambient n, the code threshold is exactly `a0=64(n+4)`.

| Requirement | Exact current theorem and scope | Status |
|---|---|---|
| (6.1)–(6.5), original measurable inputs, finite support and raw arrays | `LebesgueSampling.Input`, `.probabilities`, `.full_mean_eq`, `.marked_mean_eq`; `SamplingBoundedSupport`. Actual `p=δ⁻ⁿ vol(Full∩cell)`, `q=δ⁻ⁿ vol(G∩cell)`, original grid and actual bounded length carriers. `Representative.support_eq`, `data_eq`, `law_heq` prove the original positive support and both raw arrays/law unchanged on passing to nested Borel subsets. | P, completion new in23 |
| (6.6), (6.12), (6.13) | `SamplingDichotomy.marked_dichotomy`, `LebesgueSamplingSource.source_sampling`: actual low positive-cell count or high half-mass is derived from the original Wg. No high-mass oracle is supplied. | P |
| Nested independent selection and (6.14) | Actual finite product law has states q,p−q,1−p; `SamplingGeometricFailure` and `SamplingSourceGeometricFailure` prove full-density failure≤`2 exp(-mu/12)` on the same law. This is the exact induced selection law of independent uniform thresholds, without separately constructing continuous uniform random variables. | P/A |
| Actual ball tests and (6.15)–(6.16) | `SamplingBallTests`, `SamplingLengthInput.Input.ball_test`, source geometric failure: actual finite original-grid tests, enlarged-ball means, large-radius total-mass fallback, all-ball coverage, and cutoff≥cN^(1-s-alpha). Tube/test counts are geometric consequences. Total density/ball failure<1/8 is derived. | P |
| Exact theta, (6.17)–(6.18), whole projective net | `SamplingTheta`, `ProjectiveSphereNet`, `SphereNetCapTests`, `SphereNetSourceFailure`: theta is the displayed minimum, all closed theta caps are covered by actual tested closed2theta caps, count is O(theta^(-(n−1))), cap mean≤mu_g/1000, both high-cell tails≤exp(-mu_g/8), including real thresholds. | P |
| Literal (6.19), both budgets and probability>3/4 | `SamplingSourceProbability.source_sampling`, `SamplingJointProbability`; completed input `LebesgueSamplingSource.Data.Result` / `source_sampling`: actual test count≤N^(n+2), explicit displayed exponential<1/8, geometric failure<1/8, same-law positive `SampleGood` event>3/4. No independence of overlapping tests or failure events is claimed. Zero-weight outcomes are dealt with exactly. | P |
| (6.7)–(6.11) on that SAME outcome | `LebesgueSamplingSource.source_realization`, `LebesgueSamplingGeometry`, `SphereNetSampleRealization.Output`: original full counts `[c0 lambda N/2,2C0 lambda N]`, marks≥Wg/4 and relative fraction≥xi/(8C0), all-ball two ends, closed theta cap≤1/10, positive original intersections and O(1) longitudinal occupancy. Axes/lengths/directions/separation/additional cap coefficient are unchanged; no second ω is selected. | P |

The uniform scale cutoff precedes the family and varying δ,lambda,C0,xi,B,K. The full range `0<s<1`, `0<alpha<1−s`, beta>0 is covered (the theorem even permits alpha=0 and lambda≥δ^s). Only the stated fixed polylog K budget is needed for the net count; no additional polylog bound on B or xi inverse is required by this stronger final probability result. Full/marked sets are arbitrary `NullMeasurableSet`, not silently assumed Borel. Output filled cells lie in the available-cell union Ω; they are **not** asserted to lie inside the original measurable full union.

## Appendix A–B and first-step consequence crosswalk

| Source conclusion | Exact theorem and boundary | Status |
|---|---|---|
| Appendix A Lemma A.1, no-epsilon cumulative bush estimate | `Bush.cumulative_bush_product`, `cumulative_bush_square`, `cumulative_bush_linear_product`, `cumulative_bush_estimate`; `BushLengths.cumulative`. Actual high-incidence bush/near-far counting, sparse case and cap population give `c A⁻¹ δ^(m/2−1) s^((m+2)/2)M≤#union`, m>0, unequal/empty rows; no separation, comparability or two-ends premise. | P |
| A.2–A.3: prescribed actual finite bush iterations | `BushIteration.direct_pivot`, `bush_step`, `bush_stage`, `weakened_bush_step`, `weakened_bush_stage`. Actual ambient-(n+1) bush lift supplies d'=q=(d+2)/2, using the direct positive-p pivot rather than the narrower d'>3 corollary. The weakened version changes only d' to(d+1)/2, keeping q. Induction assumes the actual preceding estimate, not a scalar oracle. | P |
| A.4, limits and all n≥5 consequences | `BushIteration.bush_endpoint`, `weakened_bush_endpoint`; `BushIterationConsequences`; `KakeyaScalar.bush_substitution`, `weakened_bush_substitution`, domain/margin/limit theorems. Limits `(4n+4)/7`, `(4n+3)/7`; sparse margins `(d+8)/24`, `(d+11)/24`. Finite depth is chosen before configurations. Measurable stage/endpoint consequences use `MainMaximal.from_diagonal`; generic `LebesgueMaximal.of_borel` transfers them exactly to completed shadings, including n=5. No stronger endpoint substitutes for the prescribed induction. | P |
| Appendix B exact benchmarks and adjoint conversion | `KakeyaScalar.benchmark_five` through `benchmark_fifteen`: actual finite max-min specification over every integer2≤ell≤n, not merely a list of asserted rational values. `benchmark_adjoint_conversion` checks all exact r↔B fractions. | P, arithmetic |
| Appendix B sign table and five-dimensional caveat | `benchmark_comparison_signs`, `sqrt_two_rational_bounds`, `dimension_five_envelope_is_four`, `diagonal_envelope_obstruction`; exact positive dimensions6,8,10,11,13,15. Six/eight first stages and rational limits are checked by `diagonal_six_table`, `diagonal_eight_table`. No M5(K5) claim follows from the numeric row. | P |
| Appendix B six-place decimals and historical claims | Exact radical/rational values and signs are proved. The hash index additionally checks every printed decimal using 80-digit Decimal arithmetic; this is numerical display QA, not a Lean theorem defining decimal rounding. The attribution to Zahl, HRZ and Katz–Tao and claims about priority/literature are not kernel theorems and were not independently reverified in this finite audit. | Presentation/provenance qualification |
| Combined Theorem2.2; first-step proposed theorem (2) | `SixDimensionalCore.base/lift`, `SixDimensionalAllAngles.two_ends_estimate`, actual two-ends globalization, `SixDimensionalUnrestricted.discrete`, `LebesgueMaximal.six_first` / `six_first_step`. Exact D=33/8,C=15/4,margin11/24; literal arbitrary-position measurable shading conclusion and input implication. The implication premise is unused because the same constructive development supplies an unconditional seed. This is not by itself an operator norm statement. | P |
| First-step Lemmas1–5 and7 | Lemma1 is the full Gaussian row above; Lemma2/angular refinement is combined§3; Lemma3 is the completed continuous seed/hairbrush reviewed separately; Lemma4 is the normalized source marked theorem; Lemma5 is the full source sampling range; Lemma7 is actual localization. | P within stated geometry/analytic reviews |
| **First-step Lemma6**, line432: uniformity for an original logarithmic two-ends constant | The initial418 interfaces fixed B before c, but final427 `LogarithmicTwoEnds.estimate`, `WideLogarithmicTwoEnds.estimate`, `LogarithmicLengthEstimates.estimate` and `SixDimensionalLogarithmicLengths.source_notation` now construct the uniform original log-B conclusion. The final source-N consumer permits arbitrary fixed row multiples and bounded original lengths, derives the cap from original separation and retains E,N,lambda,M unchanged. | P final427 |

## Initial418 findings and remaining verification limits

1. **Resolved in final427:** the uniform logarithmic-B all-angle consumer identified above is now compiled/audited, including its fixed-row/length conventions. The proved argument uses auxiliary localization: stored density lower and original two ends give `rho^beta≤2B rho^alpha`; taking beta=alpha/2 forces only a logarithmic radius loss. The previously proposed plan is now an actual theorem chain, with all constants before original configurations. No change to the already proved main unrestricted result was required.
2. **Resolved in final427:** SourceLocalization and SourceLocalizationGeometry now supply the literal same-output dyadic statement and actual top cover. Its source hash is `8bb4be461f60b9625f83b5bdfb1c9cecfc5eb76b40fe39ce4627b043cc8dce88`; read-only review `source_localization_finite_review.md` found no defect. This is an exported-witness qualification, not a failure of the constructed radius or its bounds.
3. Await the parent’s exact frozen integrated build, all-local declaration dependency audit, regression result and delivered archive. The new source-scope adapters must not be called delivered merely from individual builds.
4. Preserve proof-method and attribution distinctions: ceiling versus floor capacities, Turán versus random ordering, chosen projected labels versus full projected-cube cover, discrete nested law versus explicit continuous uniforms, and a proved 5D seed versus reproducing Wolff's historical proof. They close the required consequences but do not reproduce every prose line literally.
5. Do not convert this bounded finite/probabilistic finding into independent validation of all mathematics, a claim of priority, a new Kakeya set theorem, or an operator claim inferred just from a shading predicate. Parent/analytic/geometry reports cover their actual interfaces separately.

The hash index SHA-256 at writing is `f703ff09cb6e454149a73b5d3733e76ba70d29874ecd581eb15b392d1291b144`. Principal new sources: CumulativeFixedError `266e9eb0435e9e665335b633e07301ffc23dcc5d06ac911f0c9d29742a7ba961`; LebesgueSampling `ad78a4007b9bb51323e504fa7137b8b3fadadc3e8e24a92229685e0767ca34c3`; LebesgueSamplingSource `c311e49f0316f1b0572ce686dbd01851dfc3d2e9258012ee15da537483f02262`; LebesgueSamplingGeometry `e1e6c73e98f22678db75e206db74d079ba5b6d9647e1e6c001e2bea0b3aa6f29`. Every referenced selected module and source input is identified in the companion index.

## Post-freeze update: localization now positively closed

`SourceLocalizationGeometry.unit_source_notation` is clean-built at SHA`cbc68cc2b665b643046bde76acd81289efda033dde4745c4c75cee8202ba7c96`. Together with the unchanged SourceLocalization it now exposes the SAME output’s dyadic index, cM/log(2N)^2, both density inequalities, original-cell support and all relative two-ends tests, directly from original width-one admissibility/comparability at N≥2. Its midpoint unit-ball cover is derived; there is no top-cover or desired-selection premise. Independent review `source_localization_geometry_finite_review.md` found no issue. This closes that exported-interface requirement in development, while both modules still require later frozen integration. It does not retroactively place their statements in the 418-source freeze.

## Post-freeze update: original logarithmic two ends now constructed

The previously identified varying-B analytic gap is positively closed in development by `LogarithmicTwoEnds.estimate` (SHA`569da09b05110e964f820042b223f08b1ba0ed42f3a9dbf0aa2a972c5d0cf075`) and `SixDimensionalLogarithmic.source_notation` (SHA`1e1af2d07c92c93f0aa8b291bea16ecb95e3da0e60fb9e89a9ed1aac0bafd842`). Both are clean-built and exact-source audited using standard foundations. The final six-dimensional theorem derives the cap coefficient from the original separation, keeps original E,M,N,lambda, and gives precisely `c*N^(33/8-eps)*lambda^(15/4)*(M/N^5)≤E` with one c before every original configuration and log-B value. Final-byte independent review `logarithmic_two_ends_finite_review.md` found no issue.

`WideLogarithmicTwoEnds.estimate` additionally constructs actual same-index row trimming for arbitrary fixed positive lower/upper density multiples and preserves that log budget and density power. It is clean/audited at SHA`129fab43489452c389ffa12e7f47c15c35b2384895edc36e41c6ac159eb7c6fa`; detailed scope is in `wide_logarithmic_two_ends_progress.md`. Scalar is assembling the remaining separate fixed-original-length convention transport; these new consumers do not silently treat arbitrary original axes as unit axes. None of these additions belongs to the frozen418, so the older snapshot's R classification remains historically accurate and later integration/publication is still necessary.

## Final427 addition: all fixed original log-B conventions

Read the complete final `LogarithmicLengthEstimates` at SHA`6a4c9c352ea8d5769beea554d3b3a0fccaeec492297ed7bf08d2bc1c721b9068` and `SixDimensionalLogarithmicLengths` at SHA`a4b219785138dc9b83af8d790eb4ce0a8cd271277e52f6470652da5119f891dc`. Exact-source audit records are PASS5/13/14 and PASS2/2/2 (named/local-theorem/all declarations), standard foundations only. The final source_notation fixes geometry,lengthUpper,c0,C0,B0,alpha,b,epsilon before c and all original data; its literal conclusion is `c*N^(33/8-epsilon)*lambda^(15/4)*(M/N^5)≤#original union`. Original separated directions supply the ambient cap coefficient internally.

The common isotropic dilation uses W=max(1,lengthUpper), changes delta and lambda to delta/W and lambda/W and keeps every original integer label and union cardinality. Actual row ratios are unchanged before the proved fixed-multiple trim. The fixed budget inflation max(1,(log2)^(-b)) handles log(2/delta)<1; original log(2/delta) is then bounded by the actual rescaled log(2/(delta/W)). Alpha is weakened to min(alpha,1) with correct radius-power direction, the fixed W loss is paid, and the exact real-power scale/density factor is absorbed before configurations. No normalized two-ends, support, desired population or cap conclusion is supplied as a new premise. These final signatures close the earlier fixed-row/length qualification.

All nine additions are now members of the427-source freeze; earlier “outside418” statements above describe the intermediate history, not the final development state. The final index verifies every427 source hash and records the initial418 manifest separately. The outstanding release gate is integrated verification/publication, not an unbuilt consumer in this assigned scope. Historical attribution, decimal-rounding presentation and the bounded evidence-strength qualifications remain as stated.
