# Actual radial pencil and cap-coefficient sharpness

Two new standard-only modules close the standalone sharpness assertion after (4.29), combined.txt lines833–844. No original/frozen source or registry was edited.

RadialPencil constructs the literal slope index set Fin k→Fin N, with slopes given by the rational grid i/N in [0,1]^k. The graph directions are actual normalized unit vectors in ambient k+1. The proved inverse projective graph chart on slope norm≤k gives separation at delta=1/[2(k+1)^2*N]. The population is exactly M=N^k, hence delta^k*M=(1/[2(k+1)^2])^k. Positive pencil meshes occur arbitrarily close to zero. No packing lower-bound or population premise is supplied.

Every actual unit tube has original base zero and its original finite shading is the singleton cell label zero; all axes pass through that cell's center. The finite union cardinal is exactly one and the total incidence cardinal is exactly M. The family is admissible, bounded, separated and comparable at density lambda=delta in the actual declared grid convention and fixed geometry(width,separation,radius)=(1,1,1). It is packaged both as a literal ShadedConfiguration and CumulativeConfiguration, with cumulative density s=delta. This is a grid-shading construction, not an additional claim that a whole high-dimensional cube lies in an unexpanded radius-delta physical capsule.

The existing actual full-dimensional packing theorem supplies CapBound delta k Cpacking. The upper cap-radius condition r≤1 then proves CapBound delta m (Cpacking*delta^(m-k)) for every m≤k, with coefficient≥1. Thus the claimed fractional cap coefficient follows from actual separation and packing, not a new cap/count oracle.

RadialSharpness exactly evaluates the proposed source expression, retaining both positive source errors:

`c*A^(-gamma)*delta^((m-3)/2+eps)*s^((m+3)/2+eps)*M`

as `c*Cpacking^(-gamma)*(1/[2(k+1)^2])^k * delta^((m-k)*(1-gamma)+2eps)`. For 1<m<k and gamma<1, it fixes eps=(k-m)*(1-gamma)/4>0 before the proposed uniform c. The remaining power is -(k-m)*(1-gamma)/2<0. The explicit arbitrarily small pencil mesh then makes this expression exceed the actual union cardinal one for every c>0. The argument proves counterexamples and negates the uniform proposed bound separately for actual comparable shadings and actual cumulative shadings. In particular gamma=1/2 fails even with a sufficiently small fixed positive error.

This proves the unrestricted cap power cannot improve beyond linear A^(-1) in this source-form sense. It does not claim fixed unit two ends for the pencil and does not contradict the two-ends A^(-1/2) estimate.

Both sources are frozen and clean-built with zero diagnostics. Exact-source audits PASS: RadialPencil36 named declarations, SHA256 eace03a56f7241f657cb18868452ad88ec367e67f68a543e89a6b44f960fdb6a; RadialSharpness7 theorems, SHA256 85c786911547c5f41ba61519c05b586c6f5976478628b94efcdcf7e24f6da52f. Every declaration uses only propext/Classical.choice/Quot.sound as needed; no custom axioms or sorry. Source-prefix, hash and expected-name checks passed. Audit logs/source/meta use the corresponding module-name `_full_source_audit` prefix in audit_work.
