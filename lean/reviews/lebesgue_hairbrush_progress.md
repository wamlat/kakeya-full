# Completed-Lebesgue marked hairbrush formulas

`LebesgueHairbrush.lean` is clean-built and frozen at SHA-256 `02c63fed02ad8e4679b198ea2d9e0cefcaa8bea4accc933539214225867b8c96`. The unchanged-source environment audit passed all 25 local theorems and 39 local declarations, including every generated proof-bearing field. Only `Classical.choice`, `Quot.sound`, and `propext` occur. There are 10 named source theorems. The exact audit is `LebesgueHairbrush_operator_audit.json` with its unchanged-source log.

The new public consumers are `broad_hairbrush_squared`, `broad_hairbrush_linear`, and `real_cap_hairbrush_linear`. The first two prove the squared and literal square-root forms of source(4.8); the third proves source(4.16). They replace only the Borel measurability hypotheses on original full shadings Y and the original good set G with `NullMeasurableSet`. Conclusions retain the ORIGINAL individual, marked and union masses, and the exact same physical exponents and dimension-dependent constants as the proved Borel kernel. The latter coefficient retains A^(-1/2) and upper-density^(-1/2).

`representatives` constructs Borel subsets of the original full shadings and a Borel subset of G, and then removes ONE measurable null superset of every original/full-representative row mismatch from all these sets. At every retained good point all original tube membership bits agree simultaneously. `Representative.broadness` therefore transfers the ORIGINAL pointwise small-cap/total-multiplicity inequality exactly, not merely almost everywhere. Individual masses, arbitrary ball-intersection masses, total mass, good marked mass, and union mass are unchanged by actual a.e. equality. No finite measure of G is assumed or needed; full row finiteness follows from the unchanged carrier hypotheses inside the original geometric theorem.

The actual geometric hairbrush theorem constructs stems, bristles, bins and energy estimates internally. None is a new hypothesis of this adapter. The explicit fine-scale assumptions remain 88δ≤rθ, 0<r,θ≤1, B r^alpha≤1/2 and the logarithmic/multiplicity-depth bounds from the raw theorem. The all-scale continuous all-angle result(4.4), variable-length convention and logarithmic density-ratio extension are separate completed-measurability consumers, not claimed to follow solely from this fine-scale wrapper.

All frozen406 files and registries are unchanged. These are verified additions for the subsequent integrated snapshot. The independent read-only scalar review is tracked separately.
