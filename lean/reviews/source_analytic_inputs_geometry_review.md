# Separate review of source analytic-input conventions

No mathematical statement or proof defect was found in the exact frozen sources below. All five requested hashes match the owning agent’s freeze notice. This is a read-only mathematical review; compilation and axiom audits are recorded separately.

The cumulative configuration keeps the original family, cells, scale and cap coefficient, and comparable row lower bounds prove cumulative mass at s=lambda. In `LiftedCumulativeInput`, the desired scale error epsilon is split into two equal parts. On a nonempty family, the strict upper comparable bound and a positive integer row imply lambda>=delta/2. Thus the source’s extra lambda^(epsilon/2) pays the missing delta^(epsilon/2), costing only 2^(-epsilon/2). Multiplication preserves inequality directions for every real q because lambda>0. Empty families are handled separately. This proves the needed comparable lifted input from the actual cumulative source statement, whose empty rows and arbitrary nonnegative cumulative density are retained in the definition.

`DiscreteScaleCompletion` is a genuine fixed-large-mesh estimate: the actual projective cap cover bounds M/A by a fixed multiple of delta^(-m); every nonempty comparable family has an occupied original cell. On delta>=a>0, each scale power is bounded by max(1,a^exponent), while lambda is in [a/2,1]. This last interval also controls negative density powers. The coefficient is fixed before every configuration. Its minimum with the supplied small-scale coefficient completes the full range, without adding a positivity restriction on m,d or the density exponent. The a>1 case is vacuous and causes no logical problem.

`SourceAnalyticInputs` assumes the estimates only at delta<=1/2, as eccentricity N>=2 in (5.4)/(5.5). Base and two-ends inputs have an absolute fixed cap ceiling and no inverse-A conclusion; actual same-scale thinning supplies the latter with one population loss. The cumulative lifted transfer uses epsilon/2 before actual data, then fixed coarse completion. The resulting Proposition 8.1 wrappers preserve max(D,C), including C<1, and the final measurable statement uses original tube-volume normalization.

The two `SourceMarkedPivot...Inputs` modules are direct compositions with the already proved source-minimum-kappa pivot. Their coefficients and geometric thresholds precede all actual delta, lambda, xi, B, theta, M, positions and marks. In ambient n=k+2 the displayed kappa exponent is 5(n-1)+6q+12; delta and lambda powers match (5.3), and N=1/delta reverses only the scale power. The union form fixes A0 before choosing its coefficient and threshold. No product/minimum-kappa comparison is used.

The remaining original-data normalization boundary is honestly visible: `AdmissiblePivotSlabs.Hypotheses` still requires exact delta direction separation, normalized comparable row counts, positive family size, B>=1, bounded bases, and the stated marked-row cap fraction. These files weaken only the analytic-input conventions. They do not claim arbitrary fixed marked comparability or separation normalization has already been constructed. That distinction agrees with their headers and should remain in coverage summaries.

Reviewed SHA-256 values:

- `LiftedCumulativeInput.lean`: `01cc887125d6677b4f015026c6e9af766fba7b1244196db7e2a6a272aed548cc`
- `DiscreteScaleCompletion.lean`: `2c9752ba6d09715aa2025da4e030ecf0f68087122c7ee4c70f66a30bb6aa5aba`
- `SourceAnalyticInputs.lean`: `0e41e594931a2ca53beebfc0297e6ca99b0d1d09c5eedb0ccb1e980a4c40b322`
- `SourceMarkedPivotInputs.lean`: `f25e37dc723c01a6a43de4700dc816604af4d6d163a66ba15b1efc9e15d1fed7`
- `SourceMarkedPivotSmallScaleInputs.lean`: `c776d76661bd93493c9c4a2aec1e9fff799cff780dcb3ab6c4dc5a9f12942a5f`
