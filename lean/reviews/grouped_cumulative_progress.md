# Actual grouped cumulative pruning

`GroupedCumulative.lean` contains 13 compiled theorems. The main exported theorem `discrete_pruning` has a positive constant chosen before even the finite group type and group/color encoding, as well as before the tube counts, scale, cap coefficient, actual tube families, and incidence set. Its assumptions are the normalized `DiscreteEstimate`, p≥1, and actual geometric admissibility/boundedness/cap/separation for each group/color family. The finite group encoding by `(coarse group, direction color)` must be injective.

The incidence records are actual unique triples `(group, tube index, grid cell)`. For every arbitrary restriction T of the original records S, the module constructs its actual tube shadings by finite filtering and projection. It proves exact total incidence and exact support-cardinality identities, including empty groups and empty individual shadings. The group densities are the actual normalized incidence counts. Applying the proved cumulative estimate to these concrete families and then weighted Jensen yields (5.27) without a number-of-groups loss.

The group/color support encoding has exactly the same cardinality as the colored coarse support. Thus the proved grouped bound supplies the analytic premise of `GroupedIncidence.calibrated_pruning`. The final theorem constructs a retained incidence set with strictly more than half the original mass and actual uncolored group-cell energy at most H times the retained mass. With `N=1/δ`, `ρ=δ|S|/Q`, and `a=c A⁻¹ δ^(m-d+ε)`, the cutoff is `H=2^(p+1) J N/(a ρ^(p-1))`, matching the manuscript's exponent calibration. Positivity of the total original tube count is derived from the nonempty incidence set.

Scope: this closes the finite/analytic assembly of §5.6 once actual valid lifted group/color families and their initial incidence records are available. It does not silently supply the preceding endpoint/output selection, actual lift transport into translated grid cells, or common slab/pivot witnesses. These remain geometric pipeline interfaces handled by the separate lift modules. Distinct original slab/pivot group labels are retained in the energy.

Compilation and axiom audit: clean Lean 4.33.1 build; exported dependencies are only `propext`, `Classical.choice`, `Quot.sound`. No `sorry`, custom axiom, per-restriction analytic-support oracle, assumed simultaneous good event, or assumed retained-mass conclusion appears. Product decidability instance differences are reconciled locally using their proved subsingleton property; existing frozen modules were not edited.
