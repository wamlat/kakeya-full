# AnisotropicSamplingInput independent statement review

Read-only review of the frozen source with SHA-256 `92ad6a8897b33f82eb926cedb716f6f40686377bbabbc22d5542bc026b6d2fd4`. No edits or repeated source/verifier audit.

No substantive defect found. The actual source family, full sets and marks are consistently used through a marked-mass segment choice, one marked-mass color, a full-density class chosen by surviving marked mass, and a proportional marked filter against ALL original image-G reference rows. It does not claim that color selection alone preserves broadness.

With segment count N, fixed palette size P and e=eta/(NP), the chosen color carries at least e*(lambda*delta^k/tau^k)*M of marked mass. The density-class output has physical full mass between density and twice density, and lambdaNew=density/[2*(delta/tau)^k]. Thus e*lambda/4 <= lambdaNew <= lambda <=1, with fixed sampling constants c0=2,C0=4. The apparently doubled marked-fraction parameter is correct: retained marks have fraction e/[8(D+1)] of full mass, while full mass is at least 2*lambdaNew*(delta/tau)^k per retained tube. Hence the Input marked coefficient is exactly e/[4(D+1)], not an unjustified strengthening.

The returned Fnew is the actual index restriction of J, whose segment choices were made using G. The selected positive full mass on T proves that every retained index has the chosen color; this is what supplies exact delta/tau separation. The inherited cap bound and fixed base bound refer to the same Fnew. The full output Z_i stays inside the common normalized image of its exact original Full index; H_i is an actual subset of Z_i and, by its definition through Oc, of the corresponding image-G mark. The union-volume bound has the correct tau^(-k) Jacobian in ambient k+1.

Every SamplingNormalizedMeans.Input field is constructed, including normalized density, count positivity, actual full/marked supports and measurability, total marked mass, all needed physical two ends, and pointwise-to-AE marked broadness. Bnew=4B/e and Knew=K*[8(1+2angular)^2]^beta*8(D+1)/e match the preceding selection and angular transport losses. No probability, expected-mass, sample-outcome, or desired estimate hypothesis has been substituted for these constructions.

Remaining scope is explicit: a genuine original common parallel box, original cap localization/separation/real-cap data, measurable full/marked input and its original mass/two-ends/broadness bounds are inputs. ActualSpatialMarked.common_base_box and SpatialMarkedGroups.construct now derive the common-box and per-box mass side from actual base partitions. This module does not provide the fixed logarithmic conditioning budgets, choose the sampling low/high branch, compare sampled support counts against original grid-piece counts, or sum the eventual estimates. Those are downstream integrations, not flaws in its statement.
