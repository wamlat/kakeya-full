# Grid hairbrush and actual spatial localization groups

6 September 2026. Continuing formalization; full seed and pivot remain incomplete.

`GridMarked.lean` constructs the actual width-normalized family of measurable shadings and the actual measurable marked cell set. It proves the marked mass equals (δ^n/W^n) times the genuine marked incidence count, full mass uses exactly the same factor, and the finite marked-half inequality implies the measurable one. Actual cap counts transfer only on the marked set; broadness is not incorrectly asserted elsewhere. The closed finite cap contains the hairbrush's strict stem cap. Finite two-ends and the dimension/width constants give all normalized physical ball tests.

`GridHairbrush.lean` applies the complete chosen-radius geometric kernel directly to finite shadings. Both measurable density endpoints are λ/W^n and Λ/W^n. The common δ^n/W^n factor cancels from actual marked mass and actual union measure. Its result is an actual finite union-cardinality inequality, with radii constructed internally from the original finite broadness and two-ends data. The explicit width factors are retained, not treated as scale- or density-dependent constants.

`DensityBroadnessRecovery.lean` and `RecoveredHairbrush.lean`, authored independently, then construct the density subfamily and marked set from arbitrary retained output incidences. Their direct application bounds the original full union. See their separate reports. The independent `grid_marked_hairbrush_review.md` found no defect in the exact marked set, density interval, two-ends constants or volume cancellation.

`LocalizedClusters.lean` constructs spatial groups from the actual grid labels of the common-radius localization centers. Each group union lies in a controlled ball about its grid origin. Actual lattice counting bounds the number of groups containing any physical point by a dimension-only constant. Integrating this count proves that the sum of group union volumes is controlled by the original union volume. Different origins across groups are handled by individual exact volume scaling and this proved sum; no equality between a union of unrelated images and the original union is asserted. Every original tube weight partitions exactly across the labels. The required center bound for shared-origin unit-tube normalization is derived from the labels, so every group has an actual uniformly bounded normalized family with transported two ends and exact volume.

These close substantial finite/measurable and spatial assembly interfaces. The unrestricted analytic seed still requires its angular normalization and full estimate summation. The pivot and final operator passage still have separate proof obligations. Statements involving a supplied base estimate remain explicitly conditional on that analytic input.
