# Remaining completed-measurability interfaces after the new adapters

This bounded scan follows the frozen406 geometry audit and its newly identified Borel/completed-Lebesgue distinction. It does not alter that frozen audit's as-of-snapshot conclusions. The added adapters are outside406 and still require final integrated packaging.

- Final maximal assertions: root's `LebesgueMaximal` supplies exact equivalent unit and variable-length predicates on `NullMeasurableSet`, with no change in constants or original union volumes.
- Real-cap / Proposition8.1 / measurable endpoint assertions: root's `LebesgueRealCap` supplies the corresponding ordinary, volume and variable-length formulations and the actual final globalize/endpoint consumers.
- Continuous all-angle square-root-cap estimate(4.4): root's `LebesgueSeedDensity` is the actual logarithmic/comparable density wrapper, including original variable lengths. This was assigned separately and must be classified by its own final compile/audit, not by my representative module.
- Lemma3.2 angular/spatial decomposition: `LebesgueAngularSpatial.construct` is clean/frozen/audited. It returns the literal existing original-input `MeasurableAngularSpatialLengths.Decomposition`; all actual retained sets are Borel subsets of the original completed-measurable sets, and pointwise broadness/overlap survives unchanged.
- Lemma6.1 raw sampling: finite agent owns the exact original cell-volume/support/raw-law transfer and consumer. `LebesgueRepresentatives` supplies the needed equalities and common a.e. incidence event; the final original-input theorem is not established merely by those helper identities.

The only additional numbered measurable clause identified in this scan is combined.txt:393, the measurable version of Lemma3.1. `CommonDensityLocalization.Selection` contains `localized_measurable : MeasurableSet(Y_i ∩ closedBall ...)`, which need not hold for a non-Borel original Y_i. Its other fields can transfer exactly from the Borel representative using individual/intersection measure equality. A small separate `NullSelection` record should retain the same selected original indices, centers, common radius/depth/density class, original-mass fraction, density range and all-radius two ends, but certify `NullMeasurableSet` of the literal original restrictions. It must not simply return a Borel Selection whose input is a different Y and describe its restriction as literally Y∩ball. This is an interface completion issue, not a failed localization inequality.

For non-numbered original proof displays, the raw broad marked kernel(4.8)/(4.16) still takes Borel Y and a Borel good set G. Extending that exact display to arbitrary completed-measurable Y/G, while retaining pointwise hypotheses, requires a common Borel full-measure locus where all original membership rows agree, intersecting new full rows and marks with that locus, and transporting original good mass. The all-angle final(4.4) is already covered by its separate completed consumer; this raw-kernel export is not a missing premise of that final result.

The exact §3.3 `SourceGoodMass`/`SourceDensityPruning` bookkeeping is more general than Euclidean Borel measure: theorems quantify over arbitrary measurable spaces and measures. They can be instantiated on Mathlib's `NullMeasurableSpace X μ` with `μ.completion`, whose `completion_apply` agrees with μ on every actual set. Thus the original masks need not be replaced by new masks or incur any null-set mass discrepancy. A convenience export would make that instance explicit; it does not need a new analytic proof. The fixed-geometry `SourceDensityPruningGeometry` inheritance is set-theoretic or measure-numerical, and follows from the same source masks and equal measure values.

The raw shrinking step of §8.1 also transfers through an actual Borel subset representative before applying `MeasurableDensityTrimming.exists_exact_subset`; final real-cap/measurable assertions now have direct consumers, so a second shrinking-only completed export is optional proof-route packaging. Operator assertions already allow a.e.-measurable functions; their function-level completion was not the set-valued Borel issue.

No additional completed-measurability restriction was found in the finite marked pivot, finite all-angle proposition, finite cumulative/bush statements or Gaussian projection counts: those inputs are actual finite sets of grid labels, not measurable shading sets. Historical attribution and the fixed-normalization choice of the source small constant remain separate questions.

## Follow-up constructions after this scan

The two additional consumers identified above have now been constructed in new sources outside406. `LebesgueLocalization.exists_selection` returns the exact original-intersection `NullSelection` (clean, source audit PASS16 local theorems/34 declarations, SHA714383c010dad54ecc9140b79f61f11486a9a6f215bf417f8a4d7c8c23cdb50e). `LebesgueHairbrush` is clean/frozen at SHA02c63fed02ad8e4679b198ea2d9e0cefcaa8bea4accc933539214225867b8c96; its exact squared/unsquared(4.8) and linear(4.16) use one common measurable null locus and retain the original pointwise broadness, full/marked masses and union. Its source audit and independent review are tracked separately in `lebesgue_hairbrush_progress.md`.

Scalar's `LebesgueDensityPruning` also now gives explicit original-mask completion instances, beyond the generic instantiation argument noted above. Finite agent's three `LebesgueSampling*` sources have constructed exact original raw support/weights/law and physical outputs, with individual audits handled by that agent. These additions remove the previously identified remaining completed-set consumers; they must still be reported separately from the frozen406 integrated result. No assertion is made here about a later integrated publication until root's new snapshot verification passes.
