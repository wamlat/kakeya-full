# Standalone original-cell localization

`SourceLocalization.lean` clean/frozen SHA8bb4be461f60b9625f83b5bdfb1c9cecfc5eb76b40fe39ce4627b043cc8dce88; unchanged-source audit PASS2 local theorems/2 declarations, only standard foundations. `dyadic_selection` runs the actual smallest common-radius and density-class constructions, retaining an explicit j<=J and rho=radius J j in its public conclusion. The older generic Selection proof chose this radius but its record omitted the witness.

`construct` fixes c=1/[(alpha+3)(3/log2)^2] and C=countConstant(dimension,width) before the actual family, scale and density. It constructs a Selection with count>=c M/log(2/delta)^2 and rho^alpha lambda<=nu<=C rho. The Selection itself has literal old-cell restrictions, comparable density and all-ball relative two ends with4^alpha. The upper density is proved by actual tube-ball counting. No direction separation, cap or endpoint estimate is needed.

This first wrapper leaves a top unit-ball cover explicit, matching the fixed top-scale enlargement allowed in combined Lemma3.1 and first-step Lemma7. The separate `SourceLocalizationGeometry` consumer removes this cover hypothesis at width*delta<=1/2 by the already-proved actual midpoint geometry, and its unit_source_notation specializes to width1,N>=2. `SourceLocalizationGeometry.lean` is now clean/frozen at SHA cbc68cc2b665b643046bde76acd81289efda033dde4745c4c75cee8202ba7c96, with unchanged-source audit PASS2 theorems/2 declarations and standard foundations only. Finite-agent final-byte review is saved in `source_localization_geometry_finite_review.md`. All older frozen sources remain unchanged.
