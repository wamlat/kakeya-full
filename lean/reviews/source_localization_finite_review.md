# SourceLocalization: independent finite review

Reviewer `/root/finite_formal`; read-only final source SHA-256 `8bb4be461f60b9625f83b5bdfb1c9cecfc5eb76b40fe39ce4627b043cc8dce88`. Read all 125 lines, the actual `FiniteGridLocalization.Selection` and `common_radius` interfaces, `FiniteLocalizedFamily.selected_count_log` / `selected_density_upper`, and combined Lemma3.1 / first-step Lemma7. The author reports a clean production .olean; its all-local audit was still running when this review was written. This is a statement/proof review, not a duplicate audit run.

No mathematical defect found. The new `dyadic_selection` repeats the actual radius/density-class construction while preserving its SAME radius index j publicly. This is useful: the old common-radius theorem returned j, while the later Selection record discarded that witness. The new theorem genuinely constructs both classes and a single object satisfying rho=radius(J,j), rather than assuming a separate dyadic property of an arbitrary old Selection.

Actual first-stage radius selection retains at least M/(J+1) indices. On those indices rho^alpha*(lambda/delta) is a lower bound on the literal restricted cell count and 2lambda/delta is an upper bound. The second finite weighted dyadic class retains at least a further 1/(D+1), keeps the same restrictions, and has positive common density s=delta*b*2^ell. The inequality s≥rho^alpha*lambda uses ell≥0. The proof of D+1≤alpha*log(1/delta)/log2+3 cancels lambda exactly and uses rho≥delta; it therefore introduces no log(1/lambda). Population positivity correctly supplies nonempty choices.

`construct` chooses c=1/[(alpha+3)*(3/log2)^2] and C=countConstant(k,width) before all actual families, scales and densities. The actual selected_density_upper theorem, using a selected index and physical tube-ball cell counting, supplies s≤C*rho. Conversion from seedLog to natural log uses log(2/delta)≥log2, not the false bound log≥1 at delta=1. The returned original rows have unchanged ball restrictions, comparable counts, actual support in their rho balls and every relative two-ends test with coefficient4^alpha.

Scope remains explicit: M>0, delta in(0,1], lambda>0, original actual admissibility and factor-two comparability, and a supplied original unit-top-ball cover. The cover is the source's permitted fixed top normalization; this theorem does not itself normalize arbitrary fixed original length/width conventions. Existing actual small-scale midpoint covering and geometric normalization provide that boundary. The theorem allows alpha≥0, so covers the source 0<alpha<1/2. It does not assert broadness or marked mass preservation, which is not this lemma's task.

This source is outside frozen checkpoint23's418 modules and must be integrated separately before a delivered-snapshot claim. Its exact joint dyadic/population/density conclusion is stronger as an exported interface than the separate frozen ingredients; no source from the frozen snapshot was changed.
