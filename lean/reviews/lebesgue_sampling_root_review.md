# Independent source review: original completed-measure sampling

Read-only review of all three sources, compared with the actual original `SamplingLengthInput`, `SamplingSourceProbability`, and `SphereNetSampleRealization` interfaces. No defect found. Final reviewed hashes:

- `LebesgueSampling`: `ad78a4007b9bb51323e504fa7137b8b3fadadc3e8e24a92229685e0767ca34c3`
- `LebesgueSamplingSource`: `c311e49f0316f1b0572ce686dbd01851dfc3d2e9258012ee15da537483f02262`
- `LebesgueSamplingGeometry`: `e1e6c73e98f22678db75e206db74d079ba5b6d9647e1e6c001e2bea0b3aa6f29`

This is a second AI-assisted source review. Compilation and the exact-source dependency audit are separate checks; the latter was running when this report was written. These sources are outside the frozen406-module snapshot.

The new physical `Input` matches the old input field for field, except that original full and marked rows are allowed to be null-measurable. Nested actual Borel subsets retain all individual, intersection, and total marked measures. The original a.e. broadness assumption transfers on one common a.e. membership event; the statement does not silently require pointwise broadness. Every geometric and numerical parameter remains unchanged.

The essential equality is stronger than equality of expectations. The positive-cell support is exactly equal, the full and marked arrays are heterogeneously equal after transporting that support, and the dependent `Data` records are equal. The proof fields are erased by proof irrelevance, while support and both arrays are compared explicitly. Equality then transports the actual finite product law and every law-dependent test together. `data h` itself is defined directly from the original Full/G rows, with no representative in its definition.

The public probability result preserves the uniform quantifier order: the small-scale cutoff is chosen before the family, lengths, original sets, and varying physical data. Its high alternative retains the original J, whole-sphere net, exact source theta, geometric failure below one eighth, literal angular exponential bound below one eighth, and positive-weight success probability above three quarters on that same original law. The low alternative retains the original marked means and is converted back to the actual original marked volume. It does not assume success, independence of the two failure events, or a preselected net.

`source_realization` constructs the existing `SphereNetSampleRealization.Output` with the literal original Full/G inputs and the actual successful omega. Consequently the existing original-axis, support, positive-intersection, separation, cap, whole-cell-union, longitudinal-count and closed-cap-broadness methods apply directly. It does not return a representative-family output and relabel it as original geometry.

The geometry file supplies the remaining input-dependent methods: actual tube enlargement, the unchanged per-tube source density interval, original Wg/4, the xi/(8C0) relative marked bound, and all physical ball tests down to delta. The positive C0 premise is used before division. The all-ball coefficient uses alpha at most one; the source range alpha less than 1-s with positive s supplies that condition. The whole-cell inclusion is into the available-cell union, not into the original shading union. No measurable-set replacement weakens these final original-data conclusions.
