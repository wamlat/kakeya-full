# Separate geometry review: unmarked fixed-length estimates

Read-only statement and proof review of the final two modules found no substantive defect. This review does not duplicate the author's compile/axiom audit. The generic and source-specialized hypotheses were read, together with the actual inherited cap/two-ends normalization in MarkedLengthNormalization.

The common W=max(1,lengthUpper) is fixed before delta,lambda,A and the family. Every original integer shading row and its union are unchanged; only the common mesh and bases are divided by W. The original per-index length-carrier incidence gives genuine new unit-carrier incidence, with the same fixed width. Directions are unchanged, original separation implies the weaker final mesh separation, and the smaller-scale cap theorem applies with m>=0. The normalized base bound is exactly R/W. The full-row mass inequalities are identical because (lambda/W)/(delta/W)=lambda/delta.

For two ends the actual old-ball radius is W*r. Up to radius1 the old hypothesis applies; beyond1 the total row count applies. The author first weakens any fixed alpha>0 to alpha0=min(alpha,1), valid on delta<=r<=1, before choosing the analytic constants. This permits the fixed B*W coefficient and avoids an unjustified alpha-dependent normalization factor. All constants still precede actual configurations. The explicit factor W^(-(m-d+eps))*W^(-p) correctly transports the two real powers without assuming their signs.

The unrestricted adapter intentionally requires p>0 and no original upper density or lambda<=1, matching the stronger OriginalLowerDensity theorem it invokes. The source globalize wrapper uses p=max(D,C)>0 from D>=1, so it includes 0<C<1 (and its imported stronger real-C formulation) honestly. The source angular wrapper preserves the displayed positive-p, q>=2, dimension, D<m and sparse-density-margin hypotheses; it retains actual original full two ends and fixed lower/upper row multiples. No marked data, sample or normalized-family output is assumed by either public result.

No lower original axis length is necessary for these finite integer-row conclusions: positive row density and actual carrier incidence themselves rule out an empty carrier whenever a tube is present. This should not be conflated with a theorem using actual variable tube volumes, for which a positive fixed lower length is separately handled by the new LengthTubeVolume/MaximalLengths/MeasurableLengthEstimates chain.

Final bytes read:

- UnmarkedLengthEstimates.lean: `9a2ceb7a87aa23d7b86491124c777ef6756baa6b9ab5d6a1e91c99e1275984fe`

- SourceUnmarkedLengths.lean: `1fad383899933828f56fb22a0e9e4576fdf6adabdb50aa05159de83459c03217`
