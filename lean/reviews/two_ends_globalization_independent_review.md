# Independent review of actual two-ends globalization

Root read the complete `TwoEndsGlobalization.lean` statement and proof against `FiniteGridLocalization`, `FiniteLocalizedFamily`, `LocalizedTwoEndsApplication`, `TwoEndsGlobalizationAlgebra` and `CoarseBounds`. No defect was found. Reviewed source SHA-256: `fca72eb28ade95cc8b17912eb5fee7a7637efcc765aaf6ed75476c51557f69b2`.

The mesh cutoff depends only on the fixed width. Below it, original admissibility gives literal radius-one coverage about each tube midpoint. A nonempty actual localization selection supplies all relative two-ends, comparability, cap, ball, density and count data; no selected-family premise remains. The zero-population case is discharged directly, and the existing small-scale adapter handles every remaining original mesh.

The first half of the final scale loss chooses the positive localization exponent and local error before invoking the analytic two-ends estimate and before any configuration. Actual local geometry bounds the selected density above by a fixed multiple of the radius and below by radius^alpha times original density. The scalar theorem changes only the density exponent to max(D,C), retaining set exponent D and one inverse original cap factor.

Both actual selection depths contribute exactly the recorded inverse square of the original logarithm to population. The reserved second half of epsilon absorbs that loss with a fixed positive constant. The selected old union is a subset of the original old union. All constants are fixed before mesh, original density, cap coefficient, population, localization and tube positions within the supplied normalization.

The explicit conditions m≥0, D≥1, C≥1 suffice for this reduction. The final estimate still has the usual actual `ShadedConfiguration` geometry, including its fixed bounded-base normalization; this is not yet the arbitrary-position measurable predicate.
