# Independent review of MainEndpoint

Read-only review of final `MainEndpoint.lean`, linked against `UnrestrictedPivot`, `MeasurableEstimate` and `MaximalShading`. No substantive defect found.

The actual real-cap endpoint retains the full density envelope `max(limitProfile(m),4)` for every real m>3 and adequate positive integer ambient dimension. Positivity of the slope proves the limiting set exponent exceeds three, so the measurable conversion's lower exponent condition is satisfied. The envelope is not silently replaced by the smaller set exponent.

For integer n≥6 the checked scalar limit exceeds four, and the discrete endpoint legitimately becomes diagonal. The actual occupancy/measurable and tube-volume adapters preserve that same exponent and every configuration quantifier. The six- and eight-dimensional equalities reduce the exact limit profile to `7−2sqrt(2)` and `11−4sqrt(2)`; there is no numerical approximation in those theorem conclusions.

The cap-free bounded maximal theorem derives its ambient cap condition from actual separated directions. Its inverse-cap coefficient is absorbed into a constant depending on fixed dimension and separation. This module explicitly retains fixed bounded-position normalization, whose radius is quantified before the estimate constant. It does not claim uniformity over unbounded original positions on its own.

All analytic input to these endpoint declarations has already been proved through the actual seed, unrestricted pivot, finite-stage recursion and uniform limiting passage. No caller-supplied pivot, marked shading, two-ends condition, sample outcome or published-result axiom remains in these declarations.

The word “maximal” here denotes the manuscript's measurable shaded-union assertion (1.1), via the explicitly named `BoundedEstimate` predicate. This module does not define a maximal operator, integrate over the direction sphere, or assert a strong operator norm such as (9.8). Those operator-theoretic consequences are a separate formalization boundary.

Final reviewed source SHA-256: `b2949fad8e54f338063da3a50c93aadeeff58bbdb8dda5e8641957688344e3a4`.
