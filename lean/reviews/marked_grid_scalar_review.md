# Independent scalar and geometric review of marked-grid normalization

Status: I read the six-module grid normalization and source-minimum chain end-to-end. No substantive defect was found. All six final sources are clean, frozen and exact-source audited by their author (73 named declarations, standard-only); I checked the final source hashes. The parent tracks integrated verification. I made no Lean source edits and did not repeat the author's compilation or axiom audit.

Reviewed hashes:

- `MarkedGridRefinement.lean`: `85eee7880d7bdf8d02938f15a5b3f6f789e59f15fe0e1631822533c37b8cf38a` (frozen)
- `MarkedGridPadding.lean`: `4da40cae28dd3793af6221f28d942bbdf0e83687ee244d476570d6d84f87db01` (frozen)
- `MarkedGridNormalization.lean`: `2a06f6b3c0eb9aa1c92a1365fab3b26a2eef7e00f26058ed6a7dbe378c6e6fba` (frozen)

- `MarkedNormalizationKappa.lean`: `5c57d6f836efc18aa58a265658c9b41939c1febdf750d008e26d45c4788bcd98` (frozen)
- `MarkedNormalizationAlgebra.lean`: `b00c18b6dce0d6e563d22d5e9ab34097bdd9f44b86b0f93d4776981ecadec01f` (frozen)
- `SourceMarkedNormalization.lean`: `5f222dc2d7791e3fbb8717f301d61fb1b716c4c9c020821cbf8807853e13eef3` (frozen; final bytes re-read)

The actual common refinement has odd factor q=2j+1. Distinct original labels have disjoint q^n child blocks; their centers lie inside the actual original half-open cells. The original marked cell maps to its anchor qz, whose new physical center is exactly the original center. Injectivity gives equality of the entire marked incidence row at every anchor, while nonanchors acquire no marks. Thus the one-tenth cap inequality is inherited with the same theta and original directions. This is a genuine construction preserving marks, unlike unproved inheritance after selecting tubes or deleting marked cells.

Arbitrary padding is justified by actual finite subset selection between all row anchors and all row children. Every full row retains all anchors, may add any permitted children, and has exactly K=ceil(b*lambda/delta) labels. The lower/upper original density bounds and existence of at least one original tube prove b*lambda/delta>=1, K<=2b*lambda/delta, and enough capacity when q^n>=b/a+1. The fixed depth depends only on a,b,sep; q>=2b ensures the actual new density delta*K/q is at most one. Its lower bound is (b/q)*lambda. The new marked fraction xi/(2max(1,b)) has a fixed loss and the original total marked count is exactly unchanged.

The full two-ends argument does not require a well-distributed padding choice. For every r>=delta/q, a child within the actual r-ball has its parent center within R=(n+1)q*r. At most q^n retained children lie over each parent. If R<=1, the original full two-ends condition applies because R>=delta. If R>1, the original total row count supplies the bound, using B>=1 and alpha>=0. Finally oldCount<=newCount because all anchors survive. This proves the actual coefficient q^n*B*((n+1)q)^alpha. For the source range alpha<=1, the author proves the coarser fixed factor endsFactor=q^n*((n+1)q), independent of alpha, before configurations. No small-radius ball-count or distribution premise is introduced.

All tube indices and axes remain unchanged. The refined mesh is delta/q; original sep*delta separation supplies delta/q separation by the fixed q choice. The proved smaller-scale cap adapter retains A and real cap exponent m>=0, including cap radii between delta/q and delta. Actual child displacement enlarges the admissibility width to q*(width+n/2). The normalized union has at most q^n times the original cover cardinality, so a fourth-power source transfer must pay q^(4n); the output record states the required actual inequality.

For the actual source-minimum theorem, the proved constant c*=sourceConstant(newWidth)/endsFactor is appropriate. Its inner term c*/B equals sourceConstant(newWidth)/(endsFactor*B), so the minimum can be compared to the normalized source choice without an alpha-dependent extracted constant. The outer constant only decreases since endsFactor>=1. `MarkedNormalizationKappa.choice_le_normalized_source` proves the exact inner identity and the outer comparison; `.admissible` derives every actual doubled-B physical and angular test. `.refined_tests` proves that the original twentieth-power cutoff suffices at the actual finer mesh. A fixed multiple of B must remain inside the 1/alpha power; treating its effect as a fixed alpha-uniform ratio outside the minimum would be invalid.

Current scope uses actual unit original axes and source 0<lambda,xi,theta<=1, alpha<=1, M>0. Literal comparable axis lengths, if claimed under Section 1's fixed-normalization convention, require an explicit common geometric adapter. The separate empty-family case is trivial but intentionally outside the input record, matching the source's omission. The standalone unit-axis marked normalization boundary is closed by the reviewed source wrapper. The separate bounded-length adapter is being implemented in my new files and is not included in this independent review.


The scalar transport module explicitly handles arbitrary real scale exponent U. It preserves the product of delta^U and (M*delta^m)^3 with the exact q^(-U-3m) factor, the density loss (b/q)^V, the marked-fraction loss D^(-X), the logarithmic factor (1+log(q)/log(2))^(-Z), and the q^(-4n) union-cardinality cost. Only V,Z>=0 are required for monotonic weakening. For source values p>0,q>=2,e>0, both conditions hold. All these constants are fixed before the scale and original data; there is no hidden variable B, theta or alpha in them.

`SourceMarkedNormalization.source_fourth` constructs the actual padded family and uses `SourceMarkedPivotFullRange.admissible_fourth` under proved source-input adapters. It supplies the exact chosen minimum and proved physical/grid tests, invokes the actual fourth-power theorem, converts the pivot logarithm, and applies the scalar transport back to the actual original cover. It never asks the caller for a normalized family, output population, energy, pruning or geometry conclusion. The original coefficient A is preserved and the finite index population remains M. Its T and c precede B, theta, alpha and all original configurations. The new width is automatically at least 1/12 from n>=2 and width>=0, so no positive lower original width is silently imposed.

`source_notation` sets delta=1/N with N>=2, uses the exact scale-power reciprocal and S=M/N^m identities, absorbs only the fixed A0 inverse, and concludes for the actual original union. It has exactly the displayed source powers H=5(n-1)+6q+12, xi^(p+3), log(2N)^(-(p+4)), N^(2m+3+dprime-3e), lambda^(p+2q+4+2e), and S^3. The same original marked one-tenth angular condition is used throughout; its preservation is a theorem of anchors, not a stronger premise.
