# Independent scalar review of the Gaussian collision expectation

No substantive mathematical defect was found. I read the final two sources end-to-end, and checked their exact shell, marginal-collision and cap-cover interfaces. This was read-only; the parent runs the production all-local axiom audits.

Final reviewed SHA256:

- `GaussianCollisionKernel.lean`: `53fe88861368bbcf8b4654fac0d50b0ada5f4f6ac4201ec6b85b263b86f8c69a`
- `GaussianCollisionExpectation.lean`: `dcac07519daaee00d02444be030a492ab4ecec97adc5659cb11b34536c8a25dd`

The saturated kernel is (delta/max(distance,delta))^4. For positive delta its denominator is positive, its value is nonnegative, and the diagonal value is one. The j=0 shell contains all distances <=delta; positive shells have the open lower and closed upper endpoints delta*2^(j-1)<distance<=delta*2^j. The inherited finite shell existence theorem covers every distance below the terminal scale, including all boundary equalities. The cover argument only needs a covering shell, so no unproved disjointness assumption is used.

The per-shell weight is at most (2/2^j)^4. The actual cap population at the upper shell radius is at most C*(2^j)^4, giving exactly 16*C per shell. The final family theorem derives its all-radius cap bound from the ORIGINAL cap condition: radii <=1 use it directly; larger radii use the already proved actual finite radius-one direction cover and the consequent total count bound. No independent total-population or dyadic-summation bound is a final premise. Its dimension constant is packingConstant(k) for ambient k+1, while the cap exponent remains exactly four.

The terminal depth ceil(log_base2(2/delta)) covers all unit projective chords, using the valid coarse upper bound two. For 0<delta<=1, depth+1 is bounded by (3/log 2)*log(2/delta). Thus rowCoefficient=48*packingConstant/log 2 has precisely the asserted natural-log dependence. At delta=1 the logarithm is log 2>0; there is no unhandled zero logarithm. Including the diagonal in the kernel row sum costs at most the same cap-controlled bound and does not require a separation premise in that intermediate theorem.

The actual random pair set is ordered and excludes i=j by defining the diagonal event to be empty. Off-diagonal events are exactly the previously proved Gaussian collisionEvent: one original matrix, actual operator norm <=K, both actual projected vectors nonzero, and the unoriented projected ANGLE <=delta. The count is its actual finite cardinality in ENNReal. The finite indicator identity and linearity of the nonnegative integral produce the exact expected count. No independence between different pairs or original directions is asserted or needed.

Original separation enters only where it is needed: for i!=j, F.Separated(delta) gives original chord >=delta>0, and chord<=projective angle gives a positive original angle. The already proved actual pair probability applies at that angle. Since angle>=chord>=delta, its inverse-fourth angular term is bounded by the saturated chord kernel; the max denominator is genuinely equal to the chord here. Symmetry only changes the row orientation, not the numerical distance. The diagonal pair probability is zero and is harmlessly bounded by the positive diagonal kernel.

`expected_count_bound` therefore proves the ENNReal integral of the ACTUAL ordered distinct-pair count is at most ofReal(C_K*A*M*log(2/delta)). Its constant depends only on K and fixed dimensions, before F, delta, A or M. Its public assumptions are the original unit-tube type, K>=0, 0<delta<=1, A>=0, original exact delta chord separation and original cap-four condition. There is no expected-count, pair-probability, cap-cover, total-population, shell-count, or output-selection oracle in the final statement. Empty families are handled by empty finite sums without dividing by M; A=0 is permitted consistently (a nonempty unit family cannot then satisfy the positive-scale cap test).

This is the logarithmic expectation assertion at combined.txt lines246–249 following (2.3). The subsequent positive-probability intersection with the good-direction event, actual graph independent-set extraction, and projected-grid/analytic conclusion are separate steps, not conclusions silently supplied by this module. In particular the collision event uses projected angular distance; conversion to a target chord-separation coefficient in a later selected family must use the already proved chord-angle comparison, with its fixed constant. The current result faithfully states its exact original separation normalization rather than claiming arbitrary fixed separation multiples without an adapter.
