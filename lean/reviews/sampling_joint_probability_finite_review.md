# Independent finite review: sampling joint probability

Reviewed the complete frozen `SamplingJointProbability.lean`, SHA-256 `b11335a81805da9e94d5ed0c65ca7caebf7fa153301dd9092ff22ec00f5e3939`, together with the actual `FiniteLaw.probability`, both failure-event/law definitions, `coupled_positive_support`, and every field of `SampleGood`. No source edits or duplicate audit. The matching production `.olean` is present; parent owns the exact-source dependency audit.

**No mathematical defect found.**

- `positive_avoidance_identity` is the correct complement identity on a finite nonnegative normalized law. The union of the two failures is disjoint from their joint avoidance. Outcomes outside both failures whose weight is not strictly positive have weight exactly zero by nonnegativity; discarding them changes no probability. No full-support or strictly positive coordinate-probability assumption is hidden.
- The actual same-law union bound and two strict probability bounds below1/8 give a strict joint avoidance probability above3/4. No independence of the overlapping geometric/angular events is assumed.
- The laws in `SamplingGeometricFailure` and `SphereNetFailure` unfold to the SAME coupled three-state product law with the SAME flattened p/q arrays and probability proofs. The final theorem does not combine unrelated realizations or exchange one law for another.
- Avoidance yields full-count bounds strictly inside [mean/2,2mean], strict ball upper bounds, strict marked-cell lower bounds and strict cap upper bounds. Weakening these to the corresponding `SampleGood` fields is valid. The cap ratio follows from capCount<mean/20 and cellCount>mean/2; it is exactly the required1/10 bound.
- Summing the high-cell lower bounds and the explicit high-mean half-mass premise gives the exact quarter-total expected marked mass. The proof uses the actual marked cardinality double count and retains all selected full incidences. Total full mass, mark nesting, and support are separately supplied.
- Positive outcome weight is used essentially to rule out selected full incidences with p=0 and selected marked incidences with q=0. These zero-probability states are not silently accepted. Degenerate zero means may make the enlarged bad-event probability premises impossible; the generic theorem does not falsely assert those premises for such arrays. Empty tube/cell types and high sets are handled by finite sums and the normalized law, with no division by population or mass.

**Scope:** this module is an exact generic combination and event-to-conclusion bridge. It retains the two actual probability bounds and high-cell mass condition as explicit inputs; it does not select uniform geometric thresholds or derive source support/means. Those belong to the separate source wrappers, as its documentation states. The final original-data source assembly must instantiate these bounds on these same arrays, masks, support and law. Compilation plus this bounded independent review is not a new whole-paper validation.
