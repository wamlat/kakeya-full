# Checkpoint 23: analytic source-requirement completion audit

This is an independent statement and proof-boundary review by `/root/scalar_formal`. It compares the combined PDF, the separate first-step PDF and the supplied audit with the actual Lean predicates and final consumers. It covers §1, the analytic conclusions in §§4–9, Appendices A–B, and the completed-measurable intermediate interfaces needed by those conclusions. The companion finite and geometry audits check their respective constructions in greater detail. This assessment does not infer completion from source or theorem counts.

**Finding.** The previously identified analytic scope gaps now have actual proved consumers: the fixed-single-error, one-log cumulative statement; completed-Lebesgue original shading inputs; and the first-step Lemma 6 estimate uniform for an original logarithmically growing two-ends coefficient. The last consumer also includes arbitrary fixed comparable-density multiples and bounded individual lengths. Within the source-derived analytic requirements listed below, no substantive conclusion or unproved analytic-output premise remains. The enlarged package still has a distinct integrated verification/publication gate. A clean individual source audit is not a declaration that that gate has passed.

The exact source-input hashes, reviewed module hashes, extracted theorem signatures and available matching audit records are in **`checkpoint23_analytic_completion_hashes.json`**. It freshly verifies all 427 current source hashes against the authoritative frozen manifest, all five original source-input hashes, and 92 selected analytic modules (70 with matching individual audit metadata). The signature inventory records 855 declaration locators. These numbers describe the evidence inventory, not a percentage or proof of coverage. The old 406 source hashes remain unchanged. The additional sources are now included in the authoritative 427-source freeze; no frozen source was edited in this review. The signature extraction is a locator, not a proof checker. The listed PASS evidence recompiles complete unchanged source and checks dependencies of all local declarations, including definitions, rather than scanning only theorem text.

## 1. Main measurable and real-cap assertions

Write `K(m,d,p)` for the actual real-cap discrete estimate, and write `P(m)=3+(2-sqrt(2))*(m-3)`. The actual ambient dimension is an integer; the cap parameter m is real.

| Source requirement | Exact final consumer and positive scope check |
|---|---|
| Combined §1.1, lines 48–94, (1.1), Theorem 1.1/(1.2): all measurable shadings, actual tube-volume sum, arbitrary positions | `LebesgueMaximal.Estimate n d` quantifies over every fixed positive separation multiple, every eps>0, then one c>0 before all delta, lambda, M, actual families and original shadings. It accepts `NullMeasurableSet ... volume`, not only Borel sets. It has no base-position, cap, broadness or two-ends premise. For 0<delta<=1 and 0<lambda<=1, its conclusion is `c*delta^(n-d+eps)*lambda^d*sum(volume(T_i)) <= volume(union Y_i)`, using the actual individual tube carriers and original union. `LebesgueMaximal.endpoint_formula` proves d=`3+(2-sqrt2)*(n-4)` for every integer n>=6. `MaximalPositionReduction.bounded_to_unbounded` actually constructs the spatial reduction; arbitrary positions are not assumed from a bounded theorem. |
| Exact six/eight radical and diagonal outputs | `LebesgueMaximal.six_endpoint`, `eight_endpoint`, `six_diagonal_limit`, `eight_diagonal_limit` prove respectively `7-2sqrt2`, `11-4sqrt2`, `29/7`, `37/7`. They are literal instances of the preceding predicate, with the same density exponent as set exponent. Their variable-length counterparts are named `length_six_endpoint`, `length_eight_endpoint`, `length_six_diagonal_limit`, `length_eight_diagonal_limit`. |
| Combined §1.2, (1.3)–(1.5): arbitrary real cap parameter, inverse A, correct density envelope | `LebesgueRealCap.real_cap_endpoint` has `3<m`, positive integer ambient n and `m<=n-1`, and concludes `Estimate n m (P m) (max (P m) 4)`. All cap centers are unit projective directions and all radii delta<=r<=1 are tested; A>=1 is allowed to vary after the estimate constant. The count-normalized measurable predicate has power `delta^(n+m-d+eps)*lambda^p*M`. `VolumeEstimate` instead has the required `delta^(m+1-d+eps)*lambda^p*sum(volume(T_i))`. These are different, correctly related powers, not an omitted ambient Jacobian. |
| The diagonal conclusion is asserted only where the density envelope is diagonal | `MainEndpoint.diagonal_discrete` and `LebesgueRealCap.diagonal_endpoint/diagonal_volume` apply for n>=6, where P(n-1)>4. The dimension-five numerical profile below four is not turned into a diagonal maximal assertion. |
| Fixed changes of length, width and separation, with actual original volumes | `LebesgueMaximal.LengthEstimate` and `LebesgueRealCap.LengthEstimate` fix a positive lower length, an upper length, positive width and fixed normalization before eps and c; then allow each original tube its own length in that interval. They use `SamplingGeometry.lengthCarrier` and its actual volume in both the lower-density condition and volume sum. `LengthTubeVolume` proves the needed inclusions/volume bounds. Common dilation factors and volume Jacobians are proved and canceled, rather than replacing actual variable-length tube volumes by an informal common value. The maximal predicate remains arbitrary-position; the real-cap predicate retains exactly its stated fixed bounded normalization. |

`LebesgueMaximal.of_borel`, `LebesgueRealCap.of_borel`, `volume_of_borel` and `lengths_of_borel` preserve the exact same analytic constant. They choose actual Borel subsets of the original sets with identical measure and restore the original union measure in the conclusion. The positive lower mass, geometry and cap data are unchanged. The use of real-valued volume is safe here: each original shading lies in a compact finite-volume carrier. Infinite-volume sets are not silently assigned real volume zero in a required lower-bound argument.

## 2. Cumulative density, conditional pivot and globalization

| Source requirement | Exact consumer, input quantifiers and retained edge cases |
|---|---|
| Corollary 1.1, combined lines 145–185, literal (1.6): ONE fixed error, ONE fixed cap coefficient, ONE logarithm | `CumulativeFixedError.from_fixed_input` and `source_notation` choose a positive loss constant from dimension, fixed normalization and p>=1, before the fixed m,d,error,c,A,scale cutoff and configurations. The input is required only for that SAME A and upper scale cutoff; it is not strengthened to all A or every error. The output is `cr*c*A^(-1)*N^(d-m-error)*s^p*M/log(2N) <= #union`. The error is unchanged. `linear_bin_budget` exports the actual `J+2 <= B log(2/delta)` bound without spending error. Actual integer bins include empty/unequal rows, M=0, s=0, arbitrarily tiny positive s below delta and densities above one at fixed geometric cost. There is no log(1/s) loss. |
| The second cumulative assertion, same density power and no displayed logarithm when every positive error is available | `DiscreteEstimate.to_cumulative` chooses the input error before configurations and absorbs its one logarithm into the remaining prescribed scale loss. `CumulativeLengths.from_cumulative` handles original bounded-length rows. The stronger every-error input is used only for this second assertion, not retroactively for (1.6). |
| Theorem 5.1, combined lines 905ff, literal base (5.4) and lifted (5.5) hypotheses | `SourceAnalyticInputs.Base` assumes only a fixed absolute-cap ceiling at source scales N>=2; `Lifted` assumes the literal arbitrary cumulative lifted estimate with A^(-1) and the prescribed scale/density error. Their `to_discrete` adapters construct the stronger internal interfaces using actual thinning, integer density and scale completion. `SourceMarkedLengths.source_fourth/source_notation` consumes those source-strength inputs, p>0 and q>=2, actual original marked rows, fixed row/length/width/separation conventions and the twentieth-power smallness test. It does not ask for a desired collision, output, pruning, energy or fourth-power estimate. |
| Exact marked fourth-power formula and Lemma 5.2/(5.34) | The actual closing, support and density constructions feed `ClosingEnergyAlgebra`, `PivotFourthPower` and the final source wrapper. The output retains original E, with kappa exponent `5(ambient-1)+6q+12`, xi exponent p+3, log exponent -(p+4), source `N^(2m+3+d'-3e)`, density `lambda^(p+2q+4+2e)` and S^3. `SourceMarkedLengths` explicitly covers arbitrary fixed lower/upper row multiples and individual bounded axis lengths. The raw scalar Lemma 5.2 is legitimately conditional on its displayed precursor inequalities; the final actual consumer proves those precursors. |
| Proposition 7.1, lines 1497ff, all angles, positive-p source range | `SourceUnmarkedLengths.angular`, through the positive-p all-angle chain, takes the same source Base/Lifted inputs and full original two ends, with D=(2m+3+d')/4<m and the source sparse margin `(m+3)/2-D+(C-2)/3>0`, C=(p+2q+4)/4. It permits arbitrary fixed positive row multiples, fixed separation/width/region and bounded individual lengths. Its constant is chosen before all original data. There is no original marked/angular condition or favorable sampling outcome premise. This is broader than the recursive p>=d subrange and retains every source p>0. |
| Proposition 8.1, lines 1654–1702: absolute-cap unit-two-ends input, all C>0, same SET exponent D and density max(D,C) | `SourceAnalyticInputs.TwoEnds` is the source-strength absolute-cap input, with B and alpha fixed before the configuration constant. Its proved `globalize` and `globalize_measurable` consumers give the unrestricted inverse-A conclusion. `LebesgueRealCap.globalize` gives the completed-measurable volume form. `SourceUnmarkedLengths.globalize` supplies original bounded-length arbitrary lower-density discrete rows. The set exponent stays D, including C>D; it is not replaced by max(D,C). Positive C<1 is handled by a proved density reduction. Actual aligned old-label cube groups, fixed mass selection, direction thinning with retained rho^m fraction, common-origin normalized geometry and disjoint old-union summation are constructed before the scalar estimate is applied. |
| §8.1 measurable conversion, including low density and unequal occupancies | `MeasurableEstimate` and the discrete/measurable adapters use actual cell intersection weights and retained incidence mass. The occupancy factor `w^(1-P)` is accounted for. The lambda<=delta branch is a genuine one-tube estimate using actual cap population; the positive-density branch does not replace occupied volume by raw cell count. `LebesgueDensityTrimming.construct` additionally exposes the literal exact prescribed-mass shrinking step for the original completed-measurable rows. Its output is an actual Borel subset with exact requested mass, including both mass endpoints. No upper-density assumption is left in the final measurable input. |
| Corollary 8.2/(8.2), also (1.7), lines 188–205 and 1703ff | `UnrestrictedPivot.real_cap_pivot` proves the actual implication for `3<d'<d<m`, p>=d, q>=d': `K(m,d,p)` and `K(d,d',q)` imply `K(m,(2m+3+d')/4,max((2m+3+d')/4,(p+2q+4)/4))`. The scalar domain, sparse margin, cumulative lift, all-angle step and two-ends removal are supplied. The endpoint theorem's pivot is not an oracle assumption. |

The source minimum-kappa convention is exact on its stated existential reading. A single sufficiently small constant is chosen after the fixed geometric normalization, and the SAME constant occurs outside and inside the minimum. `MinPivotLogBudget` proves the literal sharp inverse-log budget. There is no assertion of alpha-uniform comparability with another already preassigned small constant; such a statement can fail because a ratio appears to the power 1/alpha. The source expressly permits dependence on fixed normalization, so this is an explicit convention distinction, not an omitted theorem under the source's allowed normalization.

## 3. Standalone continuous and first-step requirements

These statements are checked separately rather than inferred from the final endpoint.

| Source requirement | Direct constructed interface |
|---|---|
| Lemma 3.1 and first-step Lemma 7: actual common dyadic radius, both density comparisons and cM/log(2N)^2 retained population | `SourceLocalization.dyadic_selection/construct` returns ONE actual Selection with its dyadic-index witness, original ball restrictions, retained population, `rho^alpha*lambda<=s` and `s<=C*rho`, the latter from actual tube-ball counting. `SourceLocalizationGeometry.unit_source_notation` removes an assumed top-cover certificate at literal width one and N>=2 using the actual unit-axis midpoint. `LebesgueLocalization.exists_selection` returns original completed-measurable `Y_i intersect ball`, preserves exact mass/intersection identities and every relative all-ball two-ends test. |
| Lemma 3.2 and first-step Lemma 2: actual angular/spatial restrictions, mass and pointwise broadness/overlap | `LebesgueAngularSpatial.construct`, with `LebesgueRepresentatives`, restores the ORIGINAL input in an actual variable-length decomposition. The common scale, retained pieces, original-index assignments, containing spatial tubes, logarithmic mass retention and pointwise overlap/broadness are constructed. A common a.e. incidence identity preserves mass; an arbitrary thinning is not claimed to preserve broadness. |
| §3.3 good-mass and pruning fractions | `LebesgueDensityPruning.source_fractions` instantiates `SourceGoodMass` and `SourceDensityPruning` on the completed measurable space. Outer measures, original masks, retained labels and good sets are definitionally identical. It proves the actual one-eighth deletion, three-quarter survivor and five-eighth good-mass fractions. The geometric companion's surviving density/two-ends, exact factor-two broadness and old-union overlap statements apply to those same sets, with no hidden Borel premise. |
| §4.1/(4.4), (4.23), and first-step Lemma 3 | `LebesgueSeedDensity.logarithmic_density_seed` and `comparable_density_seed` accept original completed-measurable full shadings with individual bounded lengths, arbitrary positions, real 1<m<=ambient-1, logarithmic two-ends and logarithmic or fixed density ratios. Their constants precede the original scale/density/A/family. They retain exactly density-square and A^(-1/2), not a weaker inverse-A consequence. The normalized lower bound is `c log(2/delta)^(-P) A^(-1/2) sigma^2 M delta^((m-3)/2)`. Specializing ambient6,m=5 and supplying the actual separation-derived fixed cap coefficient gives the separate first-step continuous formula. |
| Raw fine-scale marked formulas (4.8)/(4.16), and first-step raw hairbrush formulas | `LebesgueHairbrush.broad_hairbrush_squared`, `broad_hairbrush_linear`, `real_cap_hairbrush_linear` give the exact original marked-mass, density, scale, r/theta, sqrt-cap and log powers. Their fine-scale and depth tests are explicit, not erased by a weaker all-scale claim. They remove one COMMON measurable null superset of every original-row mismatch from all representatives and the good set. Thus every retained good point has every original row membership unchanged, preserving pointwise broadness, while original full, marked and union measures remain exactly equal. The all-angle consequence is not used as a substitute for these raw formulas. |
| Lemma 4.1 and Corollary 4.3: full fractional/cumulative seed | `FractionalSeedFullRange.fractional_discrete_seed`, `fractional_real_cap_seed`, `source_cumulative` cover every real m>1, inverse-A dependence and the stated scale/density errors. Actual arbitrary cumulative rows, including empty and tiny rows, are represented. The earlier m>3 restriction is not the final theorem's range. |
| Lemma 6.1 and first-step Lemma 5: original raw probabilities and actual same-law simultaneous realization | `LebesgueSamplingSource.source_sampling/source_realization`, `LebesgueSamplingGeometry`, and their literal Borel source consumers cover fixed 0<s<1, 0<=alpha<1-s, original fixed separation and upper length, original raw probabilities and actual positive support. They construct the low-or-high alternative, original ball tests, whole-sphere net and both event counts. Probability of the same positive `SampleGood` event is >3/4. Output full counts lie in the source broad band `[mu/2,2mu]`; this is not silently replaced by a narrow/row-equalized law. Marked mass is at least Wg/4; closed theta caps contain at most one tenth of actual marks; all physical ball tests and original axes/lengths persist. The available-cell support, not the original physical union, is the claimed containment. The finite completion audit provides the detailed (6.1)–(6.19) crosswalk. |
| First-step proposed theorem, lines 24–50, and combined Theorem 2.2 | `LebesgueMaximal.six_first` proves actual `M6(33/8)`; `six_first_step` proves `M6(4)->M6(33/8)` with the completed-measurable predicate. The premise is unused because the conclusion is independently proved, not because the implication is assumed. The actual volume exponent is 15/8+eps and density exponent 33/8. Fixed-length/arbitrary-position versions are named. |
| **First-step Lemma 6, lines 432–436: ORIGINAL logarithmic two ends, D=33/8, C=15/4, all fixed conventions** | **`SixDimensionalLogarithmicLengths.source_notation`** fixes geometry, upper length, positive row multiples c0,C0, B0>=1, alpha>0, b>=0 and eps>0 BEFORE c and all actual data. It concludes `c*N^(33/8-eps)*lambda^(15/4)*(M/N^5)<=E` for the original bounded-length carriers/rows, original separation/region, and ORIGINAL full two ends `B0*log(2N)^b`. The ambient cap is derived from actual direction separation; there is no cap, angular, marked, sampling, normalized-family, radius-budget or energy premise. This closes the uniform logarithmic-coefficient requirement, including wider fixed row factors and lengths, rather than only its normalized unit-axis special case. |

The final logarithmic proof is quantitatively explicit. Actual localization at beta=alpha/2, together with original full-row comparability and original two ends, forces `1<=2B*rho^beta`. The density/radius loss is controlled by `q=max(0,beta*C+D-C)/beta`; it therefore works even if the combined radius exponent is negative. The local analytic estimate receives the FIXED coefficient `4^beta`, the actual population loss is log^-2, and half the requested error absorbs the combined log power. The density exponent remains C. Wider row normalization uses actual all-index ceil trimming with a fixed relative two-ends cost. Common length dilation sends delta and lambda to delta/W and lambda/W while leaving the old integer union unchanged.

A potential coarse-scale trap is explicitly proved away: B0>=1 does not imply B0*log(2/delta)^b>=1 near delta=1. `LogarithmicLengthEstimates.factor_budget` enlarges B0 by the FIXED factor `max(1,(log2)^(-b))`, and `log_dilation` proves the required comparison with log at delta/W. No configuration-dependent coefficient is smuggled into an analytic constant. The actual final source-N consumer has been independently reviewed at its final hash.

## 4. Iteration, operator norm and Appendix conclusions

| Source requirement | Verified mathematical content |
|---|---|
| §9.1, lines 1759–1813: diagonal iteration | Actual finite-stage estimates use set update `(4n+d+5)/8`, density update `(2d+7)/4`, and positive sparse margin `(d+7)/24`. The six-dimensional sequence includes 4,33/8,265/64 and limit29/7; the eight-dimensional sequence includes 5,21/4,169/32 and limit37/7. Depth is chosen from the requested error before configurations. The endpoint is not a formal substitution at infinite depth. |
| §9.2, lines 1814–1875: envelope and real-cap limit | `Scalar`/`Endpoint` prove `a0=1/2`, `a_(j+1)=(2+a_j^2)/4`, monotone admissible stages and limit2-sqrt2. The associated set profile and density envelope are both tracked, and the base seed and pivot are actual proved inputs. The final `UnrestrictedPivot.real_cap_endpoint`/`MainEndpoint` consumers expose these actual estimates. |
| §9.3, lines 1876–1918: (9.7) to literal strong operator estimate (9.8) | `KakeyaOperator.maximal` is the supremum over ALL actual bases of normalized tube integrals. `KakeyaOperatorMeasurability` proves the measurability needed for that uncountable supremum. `OperatorRestrictedWeak` derives the indicator estimate from actual shading witnesses. `RestrictedStrong` proves indicator-only weak plus infinity to strong Lr by dyadic functions and actual monotone approximation. `StrongInterpolation` proves strong L1/strong Lr to La with actual high/low truncation, layer cake and Tonelli. `OperatorScaleAlgebra` chooses r>a and input errors before scale/function. `OperatorNorm.Estimate` has the literal Mathlib `eLpNorm` inequality with `C*delta^(1-n/a-eps)` for every AE-measurable real input; infinite norms and outputs are handled in ENNReal, not excluded by a hidden finiteness premise. `MainOperator` gives all claimed endpoints. |
| Fixed widths/lengths and normed-valued operator inputs | `LengthOperatorGeometry`, `LengthOperatorBound`, `LengthOperatorNorm` prove the supremum over ALL allowed positions and individual lengths, actual length-dependent tube-volume denominators, output measurability and the same scale power. The fixed norm factor `(W/h)^n * W^(-n/a)` is justified by actual dilation integrals. `ComplexOperator` and the generic normed-valued adapter include real/complex inputs, rather than merely naming a scalar estimate an operator theorem. |
| Appendix A.1, lines 1924–1964: no-epsilon cumulative bush | `Bush.cumulative_bush_estimate` and its square/product variants give the actual `c*A^(-1)*delta^(m/2-1)*s^((m+2)/2)*M` bound, including unequal/empty rows and tiny density. `BushLengths.cumulative` preserves original bounded-length rows. No epsilon or two-ends hypothesis is inserted. |
| Appendix A.2–A.3, lines 1965–2036: actual prescribed bush iterations, not just fixed points | `BushIteration.bush_stage/bush_endpoint` and `weakened_bush_stage/weakened_bush_endpoint` use actual bush lifts in ambient n+1. The second comparison weakens only the lifted set exponent and retains the density exponent. They prove finite stages and the n>=5 limits `(4n+4)/7` and `(4n+3)/7`, with finite-depth constants chosen before configurations. `BushIterationConsequences` derives actual maximal-shading conclusions; they are not obtained by substituting a stronger main endpoint for the specified iteration. Generic completed and length adapters preserve them. These exact generic compositions were separately kernel-typechecked in `checkpoint23_analytic_signature_checks.lean`; the fresh compiler exit code is zero and `checkpoint23_analytic_signature_checks.log` is empty (no diagnostics). The companion index records both file hashes and the command. |
| Appendix B: benchmark formula, table, signs and adjoint/norm conversions | `Scalar` implements the finite maximum over every integer2<=ell<=n, then proves the exact rational benchmark values and signs for the listed dimensions. Radical bounds are rationally certified. Positive comparison dimensions6,8,10,11,13,15 and the special six/eight diagonal values agree. The operator exponents13/29,19/37 and first-step5/11 have the correct sign convention. `exact_checks.py` independently checks the exact scalar calculations; printed decimal formatting is numerical QA, not a theorem about historical optimality. The dimension-five table row is explicitly numerical and does not assert the invalid diagonal profile. |

The source's Gaussian projection route is also actually represented by `GaussianNormalizedSeed.source_count`, with original ambient7, cap4, target ambient5, actual standard matrix, simultaneous norm/noncollapse/collision event, independent selected original directions and actual projected old-label fibers. This is relevant to the first-step's stated input; it is not only a generic ambient-seven seed asserted under the name of projection. The finite/geometry audits distinguish sufficient representation choices (selected rounded center labels and finite product sampling) from verbatim prose proof methods. Such choices do not leave the stated analytic conclusions unproved.

## 5. Required proof, attribution and release gate

The three user-authorized external declarations are exactly the registered normalized restricted-shading forms of Wolff1995, Katz–Tao2002 and Zahl2021, all in their documented integer-dimensional ranges. They do NOT postulate the manuscript's generalized real-cap pivot. The publication-to-normalized-predicate translation is explicitly part of their trust boundary, not advertised as kernel-proved. Their downstream measurable/grid adapters are proved. The principal endpoint/operator/source-construction results reviewed here have only standard Lean axiom dependencies in their available unchanged-source audits; they do not secretly depend on the three optional publication axioms.

Mathematical proof signatures do not establish priority, originality, whether the comparison list exhausts later literature, or the truth of a trusted publication normalization. Those are attribution/external-source questions, not omitted constructive analytic lemmas. The original audit and the word “proposed” in the first-step PDF are historical assessment data, not instructions or theorem premises. No independent human Kakeya referee's certification is claimed.

The analytic source requirements above are positively covered by the stated actual consumers and exact compositions. The remaining work for this assigned scope is the **integration gate**: complete the full verifier on these exact 427 frozen sources, retain the matching verifier-regression evidence, and publish matching audit/source-map artifacts. That gate can still reveal an integration failure. It is kept separate from the already completed individual source proofs and from the broader parent-owned coordination of the full finite/geometric statement map.
