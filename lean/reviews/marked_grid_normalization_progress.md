# Actual marked-grid normalization and source Theorem 5.1

All six new sources are clean-built with zero diagnostics and frozen. All six complete-source audits have passed (73 named declarations), using only `propext`, `Classical.choice`, and `Quot.sound`. Each source-prefix identity and SHA256 was rechecked against the final production bytes. No frozen checkpoint20 source, registry or lakefile was modified. Scalar agent independently reviewed the construction, radius comparison and final parameter transport without finding a defect.

## Completed source boundary

`SourceMarkedNormalization.source_notation` proves the original occupied-union formula (5.3), with actual original finite shadings having ANY fixed positive lower/upper density multiples a,b and original sep*delta projective separation. Its analytic inputs are precisely the root's `SourceAnalyticInputs.Base` and `.Lifted`, required only for N>=2: the first is absolute-cap, and the second has paired positive scale/density error and arbitrary cumulative shadings. These are converted using proved adapters. No stronger full-A base estimate or all-scale analytic premise is silently supplied.

For ambient n=k+2, the conclusion has exactly H=5(n-1)+6q+12, xi^(p+3), log(2N)^(-(p+4)), N^(2m+3+dprime-3eps), lambda^(p+2q+4+2eps), and (M/N^m)^3. The constant and the original N*kappa^20 threshold precede all configurations, N,lambda,M,xi,B,theta AND alpha. The proved range includes p>0, q>=2, 0<alpha<=1, 0<eps<=1, m,d>=0. Actual original admissibility, cap bound, bounded positions, full two ends, marked subset/mass, and the original one-tenth marked-cap broadness remain literal input geometry. The theorem concerns the actual original union; it does not infer broadness of all full rows.

## Actual normalization and fixed losses

Let j=ceil(max(1/sep,2b,b/a+1)), Q=2j+1. These depend only on fixed normalization data and are chosen before the configuration. At new mesh delta/Q:

- Original cell z has Q^n distinct actual integer-grid children in the centered box of radius j about Qz. Different parents' children are disjoint. The common anchor Qz has exactly the old physical center. The refined original union has exactly Q^n times its original cardinality.
- Every original full-row anchor is retained. An actual finite intermediate subset pads that row to K=ceil(b*lambda/delta). Capacity is proved from the actual lower density and positive integer row size, rather than assumed. No tube is discarded or reindexed away.
- New density is delta*K/Q, between (b/Q)*lambda and (2b/Q)*lambda, and at most one. The full rows have exactly K cells and hence satisfy the existing core's comparable-density predicate.
- Every original marked cell is sent to its common anchor, once on its original tube index. Mark counts, every occupied marked direction row, angular radius and broadness fraction are EXACTLY preserved. Off the anchor image there are no new marks. The new fraction xi/(2*max(1,b)) meets the required mass inequality; this is a fixed loss only.
- Original axes, directions and positions remain unchanged. The cap coefficient A is unchanged at the finer scale by the proved smaller-scale cap lemma (m>=0). Original sep*delta separation implies separation delta/Q. New admissibility width is W=Q*(width+n/2), proved using actual child-center distances.
- A new r-ball maps to an original ((n+1)Q)r-ball with at most Q^n children per original cell. This applies down to r=delta/Q. The old two-ends estimate is used when the enlarged radius is at most one, and the actual total old row count is used above one. Arbitrary allowed padding therefore has full two ends with Bnew=C*B, C=Q^n*((n+1)Q), uniformly for alpha<=1. There is no assumed balanced padding or sampled-cube overlap.

## Exact source minimum and parameter transport

The source choice is kappa=cstar*min(theta,1/100,(cstar/B)^(1/alpha)), with cstar=MinPivotKappa.sourceConstant(W)/C=1/[100*(2W+1)*C]. This is a fixed, sufficiently small normalization-dependent geometric constant, as permitted in the manuscript. The proof puts C INSIDE the 1/alpha power. It proves the exact identity cstar/B=sourceConstant(W)/(C*B), then compares only the outside factors to get admissibility for the ACTUAL Bnew and its later factor-two recovery loss. There is no false alpha-uniform comparison with the old unnormalized choice or the frozen product radius.

The original twentieth-power threshold directly supplies all actual geometric perturbation tests at delta/Q. The analytic core is then invoked on the constructed normalized family and literal refined original E. `MarkedNormalizationAlgebra.transport_fourth` returns to the old parameters with a positive fixed coefficient, accounting for Q^(4n) from the fourth power of the union count, both scale powers, the density lower factor b/Q, exact new xi, and log(2Q/delta)<= [1+log(Q)/log2]*log(2/delta). The arbitrary real scale exponent is transported by an identity, so no sign restriction is hidden. Only the density and negative log monotonicities need their explicit nonnegative exponents.

## Scope and downstream convention

The six modules close the fixed full-density-multiple and c*delta direction-separation gap in the literal UNIT-axis marked theorem. They deliberately do not modify each original axis length. Scalar agent separately owns the actual fixed-length homothety adapter and its final source theorem composition; the present source wrapper is ready for that input. All main claims above are constructive standard-only deductions, with no new axioms.

## Sources and audits

Each `NAME_full_source_audit.lean` consists of the complete exact production source followed by `#print axioms` for every named definition, record and theorem; its `.json` records the hash/names, and `.log` records compiler output. Parent production audits additionally enumerate generated declarations. Build command: `lake env lean -o .lake/build/lib/lean/NAME.olean NAME.lean`.

| Module | Audited named declarations | SHA256 |
|---|---:|---|
| `MarkedGridRefinement` | 23 | `85eee7880d7bdf8d02938f15a5b3f6f789e59f15fe0e1631822533c37b8cf38a` |
| `MarkedGridPadding` | 20 | `4da40cae28dd3793af6221f28d942bbdf0e83687ee244d476570d6d84f87db01` |
| `MarkedGridNormalization` | 11 | `2a06f6b3c0eb9aa1c92a1365fab3b26a2eef7e00f26058ed6a7dbe378c6e6fba` |
| `MarkedNormalizationKappa` | 7 | `5c57d6f836efc18aa58a265658c9b41939c1febdf750d008e26d45c4788bcd98` |
| `MarkedNormalizationAlgebra` | 8 | `b00c18b6dce0d6e563d22d5e9ab34097bdd9f44b86b0f93d4776981ecadec01f` |
| `SourceMarkedNormalization` | 4 | `5f222dc2d7791e3fbb8717f301d61fb1b716c4c9c020821cbf8807853e13eef3` |
