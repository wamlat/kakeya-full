# Independent review: literal minimum-radius pivot

Root read all558 lines of the three exact frozen production sources and compared the exported statements against combined manuscript §5.1, equation(5.3), including the displayed hypotheses and normalization conventions. No defect was found in the stated p>=1 scope.

- The construction consumes actual full original shadings, their actual marks and geometric/two-ends/cap hypotheses. Recovered pruning, sample system, selections, selected slabs, normalized spatial bounds and energy are all constructed. No count, output or desired inequality is supplied by the caller.
- `Package` makes kappa an explicit parameter. Generic selection, normalized geometry and closing consume only its positivity, admissibility and actual perturbation tests. This avoids the invalid inference from product kappa to the larger minimum; no uniform product comparison is used.
- The exact sourceChoice is one fixed normalization constant times min(theta,1/100,(c/B)^(1/alpha)). Its legal-exclusion theorem accounts for the actual recovered full-shading coefficient2B. The geometry threshold depends only on fixed normalization and dimension and precedes original B/theta/alpha/configurations.
- Lifted closing uses actual recovered families and union containment in E; its squared union estimate is weakened to E squared by monotonicity. The original base estimate is applied to the actual initial family with no recovered-density substitution in that application.
- Output sigma, population and cutoff substitution give the precise powers in (5.3): H=5(ambient-1)+6q+12, xi^(p+3), L^(-(p+4)), N^(2m+3+d'-3eps), lambda^(p+2q+4+2eps), S^3. Natural-log conversion uses the correct negative-power direction and only a fixed factor.
- `source_union` absorbs the fixed original cap coefficient before all later parameters and uses the literal original occupied union. `source_notation` rewrites delta=1/N and S=M/N^m exactly.
- The stronger base assumption `DiscreteEstimate` is an explicit analytic input, not an added axiom. The source's (5.4)/(5.5) follow in the intended applications; the novel geometric construction is proved relative to those inputs.

Residual boundary: these frozen statements require p>=1, while source §5.1 allows p>0. Do not count the full positive range as completed by these three files. The separate positive-density coarse/trimming generalization is active work. Source alpha<=1 is harmless because the proved statement permits any positive alpha. Fixed grid direction/comparability/width conventions are explicit in Hypotheses; fixed rescalings are not suppressed as variable conditioning assumptions.

The agent's exact full-source audit independently confirms clean compilation and standard-only dependencies. Root checked the hashes below against the audited freeze. No frozen file was edited during this review.

- AdmissiblePivotSlabs: `3360ffcde27905cd41fc14f4b3fcadb6d8d949a6898b2a355a514a99e0390b72`
- AdmissiblePivotClosing: `862a333a879b4788e3a689c8be5b4eef08586df7d4e0ae83324146ba6691a64d`
- SourceMarkedPivot: `8935fef9258325db14d34334d684b693dd364027aa2e9a3518a45925032c1da0`
