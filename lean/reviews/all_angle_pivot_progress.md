# Complete angular pivot under full two ends

AllAnglePivot.lean is frozen, clean compiled, and complete-source audited: three source theorems, 3 local declarations, standard foundations only. SHA256 442d13e8f9a331edcc59532bfa61f1028c9eddf9186d2530b88cdb128c8a5cf4.

The theorem all_scales gives, for every fixed width, base radius, full two-ends coefficient B>=1, positive two-ends exponent alpha, and eps>0, a positive c chosen before all actual finite families, scales, densities, cap coefficients and populations:

`c A^-1 delta^(m-D+eps) lambda^C M <= #originalUnion`,

where D=(2m+3+dPrime)/4 and C=(p+2q+4)/4. The original family has delta-separated directions, the every-scale real-m cap bound, comparable full shadings, bounded bases, admissible old cells and original full two ends. It requires the actual base DiscreteEstimate in ambient k+2 and lifted DiscreteEstimate in ambient k+3, m>=1, m+1<=k+2, D<m, p>=1, d>=0, q>=2 and positive sparse margin (m+3)/2-D+(C-2)/3.

The original tube family constructs angular Pieces, marked-mass-selected exact Ref refinements, all sufficiently marked spatial boxes, and actual joint segment/color/density sampling Outputs. AngularBoxAllCases proves each original-box bound using the SAME output; no supplied marked broadness, decomposition, population, support count, conditioning budget, sampling event or case estimate remains. AngularSpatialSummation then sums the SAME original finite cell sets, deriving density/population budgets and cancelling the actual angular overlap.

The proof selects beta=min(1,m-D)/2 before configurations, splits the error equally between local estimates and summation, and handles empty original families directly. The preliminary logarithmic-range theorem requires log(2/delta)>=1. all_scales removes that restriction: it enlarges only fixed width and radius, proves the physical-density normalization in ambient k+2>=2, and uses an actual ShadedConfiguration with CoarseBounds for the remaining original scales. The original family, shadings, density and cap coefficient remain unchanged.

SixDimensionalAllAngles separately specializes this theorem using the already proved standard-only six-dimensional base and actual seven-dimensional lift. The exact bound has delta^(7/8+eps), density lambda^(15/4) and sparse margin11/24.

## Remaining mathematical boundary

Full two ends is STILL an explicit hypothesis. The unit delta-separation version is now complete; arbitrary fixed separation coefficients require the additional normalization adapter being developed separately. The geometric two-ends removal, general unrestricted recursion, final maximal-operator argument, and literal minimum-kappa quantitative core are not asserted here. TwoEndsGlobalizationAlgebra proves the correct conditional exponent/logarithm algebra, but it does not replace the required actual localization, common bounded spatial groups and cap thinning at the new resolution.

The new all-angle and six-dimensional theorems depend only on standard foundations; none uses a registered published axiom. Independent source review is in all_angle_pivot_review.md. The complete frozen checkpoint manifest is authoritative for publication and package verification.
