# Paper result coverage at checkpoint 20

Prepared for the 340-module frozen candidate; authoritative only after the integrated verifier reports PASS and the publisher copies that exact snapshot. Every listed new module has a clean individual compilation and exact-source dependency audit. Independent reviews distinguish statement scope from proof counts. No percentage of correctness is inferred from the inventory.

## Completed conclusions

- **Theorems 1.1 and 2.2; main real-cap/diagonal limits:** the already published `MainMaximal` conclusions hold for actual measurable shadings and arbitrary tube positions. They include the unconditional six-dimensional 33/8 theorem and consequently M6(4) -> M6(33/8), all n>=6 endpoints and the six/eight-dimensional corollaries.
- **Maximal operator, (9.7)-(9.8):** `MainOperator` and its checked analytic chain prove actual restricted-weak and strong Mathlib eLpNorm estimates for the original supremum over all tube positions, including all real a.e.-measurable inputs. These conclusions were already fully verified at checkpoint19.
- **Corollary1.1, cumulative inputs:** actual empty/unequal shadings, scale-only integer bin budgets and positive error quantification are proved.
- **Lemma2.1:** `ProjectionConclusion.source_count` exposes the exact original cumulative count inequality. The separate Gaussian projection proof route is not reproduced; its result follows from another proved seed.
- **Lemma3.1:** actual finite and measured common-radius/common-density localization, population and density budgets are constructed. The application chain supplies its geometric covers and old-cell grouping.
- **Lemma3.2, discrete:** actual angular and spatial assignments, original-cell restrictions, containing boxes, retained incidence, broadness and overlap are constructed. The measurable counterpart is now constructed for literal unit axes; its wider length convention remains separately recorded below.
- **Lemmas4.1/4.2, Corollaries4.3/4.4:** unrestricted and cumulative seeds cover every m>1; actual integral cap selection and unchanged-scale thinning are proved. The actual radial sharpness family excludes any uniform unrestricted replacement A^(-gamma), gamma<1.
- **Continuous square-root-cap hairbrush (4.4):** `MeasurableSeedDensityLengths` now proves the literal original normalized-volume bound with A^(-1/2), arbitrary fixed positive separation and width, variable finite bounded axis lengths, independent fixed/logarithmic lower/upper density ratios, and logarithmic full-two-ends coefficients. Constants precede every actual configuration; there is no position bound.
- **Theorem5.1, normalized marked families:** `SourceMarkedPivotSmallScaleInputs` proves the literal minimum-kappa fourth-power inequality and original N/L/S notation for every p>0 and q>=2, with constants before B,theta,alpha and only the displayed large N*kappa^20 cutoff. Its analytic premises are now only the source absolute-cap base and paired-error cumulative lift at N>=2. Arbitrary fixed original marked row/separation normalization remains the separate boundary below.
- **Lemma5.2:** actual constructed selected fibers/lifted configurations instantiate the closing energy inequality and exact source scalar exponents.
- **Lemma6.1:** `SamplingLengthRealization` now covers every 0<s<1 and 0<alpha<1-s, original raw probability arrays, unchanged theta, variable bounded lengths, fixed width/separation and exact original-index/full/marked outputs. The required outcome and failure bounds are constructed.
- **Proposition7.1:** `PositiveWideTwoEndsPivot.from_source_inputs` now covers every positive base exponent, source D/margin conditions, N>=2-only weaker analytic inputs, arbitrary fixed original density multiples and every fixed geometric normalization. Actual row trimming precedes construction of new angular and marked data, preserving the original two-ends coefficient up to a fixed factor. No supplied marks, broadness, spatial pieces or sample outcome is required.
- **Proposition8.1:** `SourceAnalyticInputs.TwoEnds.globalize` and `.globalize_measurable` accept the source absolute-cap N>=2-only premise and include 0<C<1. `OriginalLowerDensity.globalize` exposes the unrestricted original-cell conclusion under any fixed positive per-tube density multiple, without even needing an upper ratio.
- **Corollary8.2:** the exact recursive real-cap implication was already proved in its stated range and is unaffected by these standalone extensions.
- **Appendix A/B conclusions:** actual cumulative bush bound, both optional bush endpoint conclusions in all n>=5, and exact numerical comparisons/dimension-five caveat are proved. `AppendixEndpoints` exposes the endpoint wrappers. Separate alternative bush iteration routes are not needed for these conclusions.

## Remaining exact-scope work

1. **Theorem5.1 marked normalization.** The current minimum-kappa theorem takes original delta-separated directions and strict normalized row ratio two. The paper permits fixed positive multiples in both. Arbitrary unmarked trimming/coloring cannot be asserted to preserve the supplied marked one-tenth broadness. Active work constructs actual common-grid children and anchor marks, preserving every original tube/marked incidence, pads the full rows, derives physical ball counts and compares the source minimum with all fixed factors inside the appropriate 1/alpha power. That construction is not a theorem of this checkpoint until fully assembled and audited.
2. **Measurable Lemma3.2 variable-length convention.** `SpatialTubeCover`, `MeasurableAngularSpatial`, and `MeasurableAngularDecomposition` now construct the complete measurable angular/spatial statement for original unit-axis carriers, fixed width and arbitrary positions, with the literal cubic-log retention, pointwise overlap and broadness uniform for beta in [0,1]. A separate bounded-variable-length carrier adapter is active. The source's wider fixed-comparable-length convention must not be silently attributed to the unit-only signature.

These two outstanding standalone scope extensions do not invalidate or condition the already proved main shading/operator conclusions. They matter to a claim that every general paper statement is formalized exactly as stated.

## Trust and publication

Exactly three authorized external axioms (Wolff, Katz-Tao, Zahl) remain registered in `ExternalAxioms`; their source-normalization translation is explicitly inside the trust boundary. All novel source-scope additions and all main shading/operator conclusions use only the standard Lean foundations. The integrated verifier must check the complete frozen source inventory, every local declaration including generated/proof-bearing definitions, and the unchanged external registry before this report is published.

The older checkpoint19 inventory and the earlier independent statement inventory are historical evidence of what was still missing then. This inventory supersedes their weaker-input, p>0, raw-sampling and continuous-hairbrush boundary descriptions; it does not retroactively put these development files into checkpoint19.
