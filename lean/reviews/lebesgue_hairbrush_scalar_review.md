# Independent review: completed-measurable marked hairbrush

Reviewed the complete final `LebesgueHairbrush.lean` read-only at SHA-256 `02c63fed02ad8e4679b198ea2d9e0cefcaa8bea4accc933539214225867b8c96`. The owner's exact-source audit metadata reports PASS at this hash. No source edit or duplicate audit was performed. No mathematical or statement defect found.

The crucial representative step is sound. Initial Borel subsets have the same original full-row measures. The single a.e. row-membership identity supplies a null set containing **all** row mismatches. A measurable null superset of that set is removed from every full representative and from the measurable good-set representative. Thus at every point in the new good set, every new full-row membership agrees pointwise with its original membership. The new good set is an actual subset of the old good set. This proves the exact original near-direction count and multiplicity identities wherever broadness is needed; separate a.e. representatives are not incorrectly treated as pointwise equal.

Removing that same null set changes neither the individual full measures, any physical ball-intersection measure, the total full mass, the marked mass `sum_i volume(Y_i intersect G)`, nor the original union measure. The marked identity uses both the full and good-set a.e. identities, so it does not lose a fraction or quietly redefine the original marked mass. No finite measure of the good set is assumed. Full-row carrier containment and finiteness are preserved by actual subset containment.

The three public formula theorems then have the exact quantitative statements of the Borel proof. `broad_hairbrush_squared` invokes the actual broad geometric theorem with unchanged `M`, directions, scale, fine-scale condition, multiplicity-class/log budgets, lower density and physical two ends. `broad_hairbrush_linear` extracts the nonnegative square root and gives the original square-root marked-mass dependence, `lambda^(3/2)`, physical `delta^((ambient-2)/2)`, `(r*theta)^((ambient-1)/2)` and `L^(-5/2)`. Its positivity checks avoid an invalid reversal from a squared inequality.

`real_cap_hairbrush_linear` retains exactly the physical cap form (4.16): marked mass times `lambda^(3/2) delta^((m-1)/2) (r*theta)^((ambient-1)/2)`, divided by `sqrt(fractionalConstant*A*upper)*L^(5/2)`. The actual cap and per-row upper density hypotheses remain attached to the unchanged tube family and original shadings. All density, broadness and two-ends transfers are measure or pointwise equalities, so no extra logarithm, cap loss, density loss or fixed geometric factor is introduced.

This is the fine-scale marked-kernel statement: the explicit `88*delta<=r*theta` and original `J/L` budgets are retained. It does not claim an unrestricted-scale kernel without its coarse-scale argument; the separate completed all-angle seed already supplies the full-scale continuous consequence. Positive population is assumed exactly as in these existing kernel theorems. The construction applies to completed-measurable original full and good sets, with no extra analytic or representative oracle.

The completed-measure boundary of these intermediate formulas is therefore closed by this new source outside the original 406-source freeze. Its integration into the enlarged verified snapshot is a separate release check.
