# Independent positive-density angular interface review

Read-only mathematical review, 2026-09-06. No substantive defect found in the critical marked first-power assembly, final two-ends interfaces, or actual unmarked density normalization. This review does not repeat the source axiom audits or claim a fresh line-by-line review of every mechanically updated intermediate file.

- `PositiveMarkedPivotEstimate.construct` genuinely invokes `SourceMarkedPivotFullRange.admissible_fourth` with p>0. It constructs the original admissible hypotheses and actual geometry tests at the chosen product radius; it does not use the old p≥1 fourth-power premise. The power losses p+3,p+4 are nonnegative already for p>0. The scalar absorption uses e=min(1/2,eps/4), the correct powers -2m-3-dprime+3e and p+2q+4+2e, and original S=M delta^m with the actual cap-population bound.
- Kappa remains the documented product radius for this first-power/logarithmic-budget theorem. Fixed radius admissibility is applied to the recovered coefficient 2B; the twentieth-power cutoff is derived from the same logarithmic lower bound. This is appropriate for the angular proposition and is not passed off as the separate literal minimum-radius theorem.
- `all_scales` uses a coarse occupied-cell bound only after observing pivotDensity=(p+2q+4)/4≥1 from p>0,q≥2. Its fixed cutoff and constant precede every actual configuration.
- `PositiveTwoEndsPivot` retains literal full two ends, all fixed geometric normalizations and the explicit sparse margin. The absolute-cap and paired-error lifted premises are converted by proved adapters. `PositiveTwoEndsSmallScaleInputs` additionally consumes the root predicates required only for N≥2.
- `WideTwoEndsEstimate.normalize_rows` constructs exactly ceil(a lambda/delta) retained old cells on EVERY original index, with a=min(c0,1/2), new density delta*K/2 in (0,1], and new density at least a lambda/2. The rounding argument includes the single-cell case. The new full two-ends coefficient max(1,B*C0/a) is fixed before delta/lambda and follows from actual row-count ratios and subset ball counts. Same axes preserve cap/separation and the actual old-union subset gives the final bound.
- `PositiveWideTwoEndsPivot` weakens the density factor only after invoking this actual unmarked normalization. It makes no assertion that arbitrary trimming preserves marks or marked broadness. Thus it does not close, or silently bypass, the separate marked-input normalization task.

Reviewed source hashes:
- `PositiveMarkedPivotEstimate.lean`: `da51c59cf87d89501c21dfdfda2fa274e2655572397e2506193283e9f75802da`
- `PositiveTwoEndsPivot.lean`: `3ec19d098a90738a18978e41d5de46d06af345dbb9a97efe165350dfe3d0ca3a`
- `PositiveTwoEndsSmallScaleInputs.lean`: `6b41c018121f4b516c05fae2cdb31070f78738d173cd812fca21a2f366e46ce5`
- `WideTwoEndsEstimate.lean`: `5667f23e0b89ad3fc2906aea2b8a2365ec331ceaaf74ec679cb1dfcf0adfee51`
- `PositiveWideTwoEndsPivot.lean`: `abb31cfa8fd7c49943469fd8bf1dbdc6683c458fa3cdff935fdfd0cc933a8da8`
