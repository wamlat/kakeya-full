# Restricted indicator interpolation: first finite kernel

`RestrictedInterpolation.lean` is frozen, compiles cleanly, and passes the exact-source all-declaration audit: 15 named theorems, 44 local environment declarations (34 theorem declarations and 10 others), with only `propext`, `Classical.choice`, and `Quot.sound`. Source SHA256: `22fa05e6e61131966df936dd5059d63dd2dfeaa3fc64841b4c962450b038cdbe`.

The operator-law record contains only pointwise zero, monotonicity, measurable-input subadditivity, finite nonnegative homogeneity, and the infinity contraction. Theorems construct actual finite indicator superpositions, prove the weighted level-set cover in outer measure, and apply a restricted weak estimate solely to the individual measurable sets. For pairwise disjoint dyadic bands they construct the low/high split and explicit geometric weights `(1-q) q^(j-J-1)` whose sum is at most one for every finite set of bands. The level bound has no factor depending on the number of bands. Infinite operator outputs are allowed throughout and no output measurability is presumed.

The current endpoint is `dyadic_restricted_level`: the distribution of the actual dyadic simple function at level `2t` is bounded by the explicit weighted indicator tails over bands above an integer cutoff. This is a substantive finite interpolation kernel, not yet a strong Lr estimate. `RestrictedDyadic.lean` now constructs the cutoff and simplifies the geometric weights into the integrable continuous power bound. `DyadicStrong.lean` integrates it, while `DyadicApproximation`, `DyadicOperatorExtension`, and `RestrictedStrong` complete the actual measurable-input first stage. The subsequent strong L1/Lr interpolation remains a separate active stage.

`RestrictedOperatorLaws.lean` separately instantiates every elementary law for the actual Kakeya averaging supremum using `KakeyaOperatorLaws`; it has compiled cleanly. It introduces no operator-law axiom. Its exact-source audit also passes (1 local declarations), with standard foundations only; SHA256 `1ffa886222abc498bca0d1d9747072a040f2d6c2522efcd78becc1885ec9e376`.

Reproduction, from the development package: `lake env lean -o .lake/build/lib/lean/RestrictedInterpolation.olean RestrictedInterpolation.lean`, then `lake env lean ../restricted_interpolation_axioms.lean`. The latter is the exact source concatenated with the production verifier AUDIT command; audit outputs are `restricted_interpolation_axioms.log` and `restricted_interpolation_audit.json`.
