# Independent statement review: anisotropic high-density application

Read-only review of the frozen `AnisotropicDensityPower.lean` and `AnisotropicHighDensity.lean`, against `AnisotropicSamplingRetention`, `AnisotropicSamplingBudgets`, `SamplingOriginalCells`, and the restricted-piece comparison needed in combined PDF §7. No source was edited. This is a mathematical statement/linkage review, not a duplicate of the root source audit.

No mathematical defect or conclusion hidden as a premise was found.

`power_mass` correctly combines the density lower bound to power C-1 with the retained density-times-population bound. The resulting logarithmic exponent is exactly x*C+1. It avoids paying a separate population loss in addition to the already proved joint mass loss. `output_power` obtains both bounds from the SAME literal Retention.Output, including its actual depth bound. Its original density is positive because the output density is positive and no larger than the original density. All real powers are taken at positive bases where required, and C>=1 is explicit.

`original_parameters` fixes the small-scale cutoff and positive estimate constant before both meshes, the original full sets, the output object, population, density, cap coefficient and variable conditioning parameters. It derives the output's marked-fraction/two-ends/broadness budgets from the displayed original budgets and the actual output depth. The density exponent C=(pExp+2*qExp+4)/4 is at least one under the stated base pExp>=1 and lifted qExp>=2 assumptions. The extra logarithm is absorbed using eps/2, leaving the stated exponent m-D+eps exactly.

The old-cell comparison is real support geometry: every pre-output full set must lie in one common normalized image of the same old cell set S. Output.contained supplies the SAME normalizeBox map, yielding the pieceMap support premise of SamplingOriginalCells. That theorem bounds occupied normalized support cells by a fixed dimensional multiple of card(S); no identity between cardinality and normalized union volume, and no overlap assertion for independently pulled-back sampled cubes, is used. The final parameter A is the actual Output cap coefficient. In the newly constructed angular/spatial application it is capFactor(k,3,m)*A_original; this fixed factor still needs to be absorbed into the final constant when expressing the result using A_original.

`original_log_parameters` transports original-scale budgets only under the explicit complement inequality `(delta/tau)^a <= delta`, a>=1. The inverse-retention coefficient is reduced by a^(-xLog), and upper B/K coefficients are increased by a^(bLog/kLog), all positive fixed constants. Those choices precede the configuration. The proof derives delta<=1 and normalized-scale positivity from the actual Output input and tau<=1.

Residual hypotheses are accurately visible: two actual base/lift DiscreteEstimate inputs; D<m+1; positive alpha and beta; normalized mesh below the fixed cutoff; high *output* density at least (delta/tau)^(1/3); original-log conditioning budgets; and the actual coarse-range complement for the transported version. This is the high-density branch, not the unrestricted angular pivot theorem. The coarse normalized-scale branch, low-output-density branch, summation/recovery across all actual angular/spatial pieces, and eventual two-ends removal remain separate steps. AngularSpatialSampling now derives the full/marked spatial outputs and the old-support premise from actual refinements, but it does not by itself discharge those numerical branch conditions.

For the six-dimensional stage, the standard-only seed inputs remain DiscreteEstimate 6 5 4 4 and DiscreteEstimate 7 4 (7/2) (7/2), giving D=33/8 and C=15/4. These new files materially extend the actual high-density restricted-piece application; they do not yet establish the unrestricted M6(4)->M6(33/8) transition.
