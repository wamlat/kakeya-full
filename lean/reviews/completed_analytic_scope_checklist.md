# Completed analytic scope after the frozen-406 audit

This checklist supplements, rather than rewrites, `checkpoint22_analytic_completion_audit.md`. The original freeze lacked a literal fixed-error one-log export and completed-Lebesgue versions of several shading predicates. The following additions are outside that freeze. Their current source hashes have been checked against the individual PASS audit metadata; all recorded dependencies are standard-only. Exact hashes are in `completed_analytic_scope_hashes.json`. An individual audit is not a claim of integrated verification of the enlarged package.

## Required statements closed in development

| Requirement | Actual public completion and retained scope |
|---|---|
| Corollary 1.1/(1.6): one fixed error and cap coefficient, one logarithm | `CumulativeFixedError.from_fixed_input` and `source_notation`. Input is at only that cap coefficient and any fixed scale cutoff; output has the same error and precisely `log(2N)` in the denominator. Empty, tiny and unequal shadings are included. Independent final-byte finite review passed. |
| Theorem 1.1/(1.1), first-step `M6(4) -> M6(33/8)`, diagonal/radical endpoints, arbitrary positions and fixed individual lengths | `LebesgueMaximal.Estimate` and `LengthEstimate` replace the Borel input condition by `NullMeasurableSet`. Their same-constant equivalences and actual endpoint wrappers retain the original sets, their union measure and the actual tube-volume sum. Independent finite review passed. |
| Real-cap measurable count/volume forms, density envelope, §8 measurable globalization and fixed individual lengths | `LebesgueRealCap.Estimate`, `VolumeEstimate`, `LengthEstimate`, the same-constant equivalences, endpoint wrappers and `globalize`. The actual source absolute-cap two-ends premise is preserved. Independent scalar review passed. |
| §9.3 strong maximal-operator conclusions | No new set-completion adapter is required: frozen `OperatorNorm` already takes AE-measurable real functions; `ComplexOperator` and `LengthOperatorNorm` cover normed-valued inputs, actual averages and variable-length supremums. Their previous exact analytic review remains applicable. |
| Lemma 3.1 common-radius/common-density localization | `LebesgueLocalization.exists_selection` returns a `NullSelection` whose sets are literally the original `Y i` intersected with the selected ball. Depth, population, density, mass and full all-ball two-ends factors are unchanged. Independent scalar review passed. |
| Lemma 3.2 angular/spatial refinement | `LebesgueRepresentatives` constructs actual nested Borel subsets with one common a.e. incidence identity; `LebesgueAngularSpatial.construct` restores the original input in the actual variable-length decomposition while preserving its selected pieces, pointwise broadness/overlap and mass. Independent scalar review passed. |
| §3.3 actual density pruning and good-mass estimates | `LebesgueDensityPruning.source_fractions` instantiates the generic proofs on the completed measure. The original outer measures, deletion masks, good sets and retained labels are definitionally identical. The exact eighth/three-quarter/five-eighth fractions are proved. The geometric density/two-ends/broadness/overlap statements already had no Borel measurability premise and apply unchanged. Independent parent review passed. |
| §4.1/(4.4): continuous density-square, square-root-cap estimate, including logarithmic density ratios and fixed lengths | `LebesgueSeedDensity.logarithmic_density_seed` and `comparable_density_seed` preserve the original input, `sigma^2/sqrt A`, normalized union volume, all physical ball tests and uniform parameter order. Independent scalar review passed. |
| Lemma 6.1 original raw probabilities, full parameter range, same original geometry | `LebesgueSampling` proves exact original support/data/raw-law equalities; `LebesgueSamplingSource.source_sampling` and `source_realization` state the actual low-or-positive-probability result and original-input sample; `LebesgueSamplingGeometry` gives its quantitative physical conclusions. The public range is every fixed `0<s<1`, `0<=alpha<1-s`, with fixed separation and individual upper length. No narrow-band or row-equalized input replaces the original probabilities. The owning agent's exact-source audits passed; the parent's final coverage review should use these literal declarations. |

## Final marked-kernel addition and integration gate

The final **standalone marked fine-scale continuous hairbrush consumer** is now closed in development by `LebesgueHairbrush`, SHA-256 `02c63fed02ad8e4679b198ea2d9e0cefcaa8bea4accc933539214225867b8c96`, with a clean build, exact-source PASS audit and final-byte independent scalar review. It constructs a common measurable null exceptional set, removes it from every representative and the good set, and thereby retains pointwise direction-row equality at every surviving good point. Its public `broad_hairbrush_squared`, `broad_hairbrush_linear` and `real_cap_hairbrush_linear` prove the original completed-measurable forms of the fine-scale formulas (4.8)/(4.16), with exact original marked mass and union measure. See `lebesgue_hairbrush_scalar_review.md`. This independently covers the intermediate formulas instead of substituting the already closed all-angle consequence for them.

**The final first-step logarithmic quantifier is now closed.** The separate first-step PDF, Lemma6/(25), permits an original B0*log(2N)^b coefficient, which the earlier fixed-B theorem did not supply uniformly. `LogarithmicTwoEnds.estimate` constructs actual localization at half the original ball exponent and preserves the density exponent at a fixed local analytic coefficient. `WideLogarithmicTwoEnds` uses actual all-index row trimming. `LogarithmicLengthEstimates` proves the common length dilation and original-to-rescaled logarithmic comparison, including fixed log2 inflation at coarse scales. The final `SixDimensionalLogarithmicLengths.source_notation` proves the literal cap-free `E>=c*N^(33/8-eps)*lambda^(15/4)*(M/N^5)` for arbitrary fixed lower/upper row multiples and individual bounded lengths, with all fixed parameters before c and all original families/scales afterward. All these sources compile clean, have matching unchanged-source PASS audits and final-byte independent reviews. See `logarithmic_length_estimates_progress.md` and `logarithmic_lengths_geometry_review.md`.

The final positive requirement matrix and authoritative 427-source hash inventory are `checkpoint23_analytic_completion_audit.md` and `checkpoint23_analytic_completion_hashes.json`. They supersede this incremental checklist for current coverage. The previously discovered fixed-error, completed-measure and logarithmic-budget requirements are now actual proved consumers in that freeze. The full integrated verification/publication remains a distinct gate and is owned by the parent; no theorem-count completeness inference is made here.

Historical priority, whether cited benchmarks exhaust the literature, and the publication-to-normalized-form translations of the three authorized external axioms remain attribution/trust matters. They are not missing constructive analytic conclusions, and they should not be silently described as kernel-certified facts.
