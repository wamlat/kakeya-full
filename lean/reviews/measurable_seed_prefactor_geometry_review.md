# Separate read-only review of the measurable seed prefactor

No mathematical statement/proof defect was found in frozen `MeasurableSeedLogLoss.lean` (SHA-256 `8b69548d93d9ba766989bb7cff6f855364d69efc44377f2f94a781316b1d0670`).

The actual depth inequality gives the reciprocal logarithmic lower bounds for retained population and group density. `depthConstant` is definitionally identical to `MeasurableAngularHairbrush.seedConstant`, with original physical two-ends coefficient B and no grid-width normalization. The one-group logarithmic lower bound is multiplied only by nonnegative rate factors. The exact cap identity is used before multiplication, preserving `1/sqrt A`. The final uniform constant depends only on fixed parameters; the actual J, tau, delta, A, and density are absent from its definition. The lower/upper scale hypotheses ensure all logarithmic and real-power comparisons have their required positive domain.

This is the expected algebraic adapter for the actual measurable hairbrush statement. It does not claim or assume measurable angular pieces, density-ratio normalization, or a desired union estimate.
