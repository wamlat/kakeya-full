# Completed-measure source sampling

The three new modules are frozen, clean-built with production `.olean` files, and **PASS** the production all-local exact-source declaration audit. They use only `Classical.choice`, `Quot.sound` and `propext`. No axiom, sorry, unsafe declaration or native decision procedure was added. Root independently reviewed all three final sources and saved `lebesgue_sampling_root_review.md`.

| Module | SHA-256 | Audited local theorems / all declarations |
|---|---|---|
| LebesgueSampling | `ad78a4007b9bb51323e504fa7137b8b3fadadc3e8e24a92229685e0767ca34c3` | 51 / 75 |
| LebesgueSamplingSource | `c311e49f0316f1b0572ce686dbd01851dfc3d2e9258012ee15da537483f02262` | 6 / 10 |
| LebesgueSamplingGeometry | `e1e6c73e98f22678db75e206db74d079ba5b6d9647e1e6c001e2bea0b3aa6f29` | 14 / 14 |

The corresponding `*_operator_audit.json` and `.log` files contain every production declaration and transitive foundation dependency. `lebesgue_sampling_audit.json` is the compact summary. The 406 frozen checkpoint22 sources and shared registry/verifier were untouched; these additions require a subsequent integrated freeze.

`LebesgueSampling.Input` reproduces the original `SamplingLengthInput.Input` physical hypotheses, changing exactly the full and marked set-measurability fields to `NullMeasurableSet` for Euclidean volume. The original dimension, mesh, width, lengths, directions, bounded region, density multiples, λ, ξ, B, α, K and β remain unchanged. `Input.to_borel` uses actual nested Borel subsets from `LebesgueRepresentatives`, preserves every physical mass and ball intersection exactly, and transfers all original marked cap tests on a single common almost-everywhere membership event. It loses no mass or angular parameter.

`Representative.support_eq` proves equality of the **actual** finite positive-full-intersection support. `full_heq` and `marked_heq` prove equality of the entire original raw probability arrays after that equal-support transport. The `Data` record packages only this finite support, those two arrays, and their proved probability inequalities. `data_eq` is equality of those actual data, not an assumed equality of means; `law_heq` consequently identifies the original product law. In particular all individual pair selection probabilities, every masked count, test event and outcome are unchanged. The exact original full means and literal Wg follow as separate corollaries.

`LebesgueSamplingSource.source_sampling` chooses a cutoff before every actual family and varying physical parameter, for all `0<s<1`, `0≤α<1-s`, β>0 and the fixed polylog upper budget on K. It invokes the previously proved source theorem on the constructed Borel subsets and transports its complete result by exact data equality. `Data.Result` exposes the original low alternative or the same actual dyadic depth, whole-projective-sphere θ-net, original high cells, literal `N^(k+2)` test count, geometric failure <1/8, the exact angular exponential display <1/8, probability >3/4 on the original product law, and an actual positive-weight outcome. No probability, count, high-mass, mean or sampling certificate is supplied by the caller.

`source_realization` returns `SphereNetSampleRealization.Output F Full G ...` with the **original** completed-measurable Full/G parameters and original cell domain. It packages the very same outcome and net from that probability theorem. Its low branch is the literal original-volume `LowResult`, using Wg rather than a representative volume. No Borel representative appears in the public result.

`LebesgueSamplingGeometry` supplies the original-input-dependent physical guarantees for that output: centers lie within `(width+k/2)δ` of the original length segment; full counts lie between `c₀λ/(2δ)` and `2C₀λ/δ`; marks retain at least Wg/4 and at least `ξ/(8C₀)` of the full sampled mass; and all physical balls δ≤r≤1 satisfy the source two-ends bound with fixed coefficient `8*ballCoefficient*B`. The latter needs α≤1, which follows from the source α<1-s and s>0. The already-proved, input-independent Output methods give actual positive original intersections, longitudinal interval counts, unchanged axes/separation/cap coefficient, exact original available-cell containment and every-center **closed** θ-cap broadness ≤1/10. Whole sampled cubes are not claimed to lie within the original measurable shading union.

This closes the Borel-versus-Lebesgue input distinction for source Lemma6.1 without altering its raw arrays, mesh or joint probability. It does not change other frozen geometric or analytic predicates; their separate completed-measure adapters are owned by root/geometry. No assertion of integrated publication is made until the new exact sources enter and pass the next complete checkpoint.
