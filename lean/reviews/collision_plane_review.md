# Separate review of CollisionPlane

Reviewed the completed source of `CollisionPlane.lean` and the used `ThickCircle.indexed_thick_circle_count` interface. No mathematical defect or hidden output-count factor was found.

The constructed frame is a genuine linear isometry. It aligns the first direction, then aligns the normalized transverse part of the second direction. The proofs show both original directions have zero remaining normal coordinates; the zero-transverse case is explicitly covered. No plane-membership or frame hypothesis equivalent to the desired conclusion is assumed in the configuration-facing theorem.

The displacement identity in `direction_normal_bound` has the correct signs: subtracting the three point-to-axis residuals leaves t*w - a*u + s*v, and the normal projection annihilates u,v. Hence three errors of size 2wδ give 6wδ/κ after dividing by the legal competing intermediate coordinate |t|≥κ. `colliding_angle_thin` derives all three residuals from actual original shade incidences, uses the exact shared intermediate label from the two outputs, and obtains |t|≥κ from the legal sample and its fixed axis sign. These hypotheses are substantive geometric inputs already constructed by the sample pipeline, not a thin-plane conclusion in disguise.

Crucially, `firstIndices` has no fixed-output parameter. It takes every competing marked angle with the same original second tube and existentially quantifies ANY pair of actual legal samples sharing ANY output with the fixed angle. It then takes the image of the original first-tube index. Thus its direction set is aggregated across all shared outputs before packing, and duplicate angles or output witnesses do not duplicate direction indices. The same fixed frame applies to every existential witness.

The cap count applies actual projective-separated directions after this common isometry. The thinness coefficient is 6w/κ and the explicit geometric smallness condition is (6w/κ)δ≤1/2. The inherited thick-circle theorem counts one tangential coordinate with ψ/δ and exactly k normal coordinates in ambient dimension k+2. Factoring its explicit constant yields κ^(-k), as required for the manuscript's ambient-dimension-(n−2) loss. The shell is bounded by its enclosing 2φ cap, producing only the displayed factor 2.

Scope: `collision_shell_count` covers shells φ≤pd≤2φ with φ≥δ; the below-δ/diagonal shell must use `collision_cap_count` with ψ=δ. `CollisionIntersection.common_shading_count_max` now supplies its compatible intermediate-label bound. The smallness condition is still explicit in CollisionPlane; it follows uniformly from the existing logarithmic κ cutoff after choosing the fixed twentieth-power threshold large enough. The file does not yet assert the complete collision row or the sum (5.18).
